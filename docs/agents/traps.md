# Traps — permanent, one line each

Standing warnings that used to live in the `LATEST.md` banner and in rotated-out entries. **One line per trap; link the dated summary that earned it.** `grep` this file for the area you are about to touch. New traps: append here in the same commit as the session summary (the `## Traps` section of the summary lists them too). Never delete a line because it is old — delete it only when the code that made it true is gone.

## Composer / attachments / media
- **Composer/attachment-picker ship freeze LIFTED 2026-09-19 (owner)** — the 2026-08-19 "green repro AND explicit OK" gate is retired; keep the repro discipline. Unrelated and still binding: never `git revert 0cbf17b` — it carries the 0.1.10 identity guard and the banner removal too (`2026-08-19-session-composer-regression.md:79-87`).
- **Dependabot #174 (`file_picker` 11.0.3) is deliberately OPEN** — `web_file_input.dart` exists because of 11.0.2's DOM behaviour; no host test sees that path (`2026-09-08-session-dependabot-sweep-and-pc-health.md`).
- A video RETRY must carry the full payload (poster/dimensions), not url/key/iv/duration only — a once-failed clip reached the peer as a 220 px blank square (`2026-09-05-session-video-nits.md`).
- **A `Stack` child pushed out with a negative `Positioned` offset paints but takes NO taps there** — `Clip.none` only affects painting; the reaction chips (text and voice bubbles) answered on their bottom edge only until the 14 px room moved inside the Stack (`2026-09-26-metadata-item4-item9.md`).
- A route pushed over the chat must RELEASE the inline video slot (`2026-09-05-session-video-nits.md`).
- The WebKit `videoWidth == 0` probe fix is **NOT iOS-verified** — owner owes a recording from the iPhone PWA camera (`2026-09-05-session-video-nits.md`).
- The attach picker must stay EXEMPT from the passcode lock, or a 0 s auto-lock loses the pick (`2026-09-05-session-video-merge-e2e-fix.md`).
- **Composer focus must NEVER move the bar:** `bottomInteractivePadding`'s `keyboardVisible` may only read real insets (`viewInsets.bottom`/shared visualViewport) — folding `_focusNode.hasFocus` in (H1) dropped the input row by the whole resting clearance on every tap, behind the bottom nav on desktop web (`2026-09-12-session-composer-focus-drop.md`).
- **Sibling `SafeArea`s each pay the FULL inset; nested ones do not** — a `SafeArea` strips the inset only for its OWN subtree (`MediaQuery.removePadding`), so wrapping a banner stack in one NEXT TO the tabs left the tab subtree seeing the whole `padding.top` and the header spent it twice (phone-measured: title top 269 → 165 px at inset 104). The obvious falsification test is wrong for this reason — a nested `SafeArea` adds nothing, so the red case must model the SIBLING shape (`2026-09-14-session-apk-field-test-list-preview.md`).
- **`InteractiveViewer` claims the vertical drag whenever `panEnabled`** — a swipe-to-dismiss over a zoomable image must arm ONLY at rest scale (`panEnabled` true only while zoomed): one gesture owner at a time, decided by scale (`2026-09-14-session-apk-field-test-list-preview.md`).
- **An `AnimationController` declared as a lazy `late final` is CONSTRUCTED INSIDE `dispose`** if nothing ever touched it, and `createTicker` then throws "Looking up a deactivated widget's ancestor is unsafe" — build it in `initState`. Caught by a viewer opened, zoomed and closed without ever dragging (`2026-09-14-session-apk-field-test-list-preview.md`).
- **The `delete_forever` action tile wipes the thread for BOTH users, and the hold is an accident guard, not consent** — 1500 ms under a progress ring, abortable on release, but it fires `clearChatHistory` → `deleteAllByConversation` (no user filter, media unlinked, irreversible, pre-link history included). It now carries a `tooltip`, the ring caption states the scope, and completing the hold opens `widgets/dialogs/clear_chat_dialog.dart`. Do NOT wire the gesture straight back to the emit (`2026-09-14-session-delete-convergence.md`).
- **An unauthenticated upload route must not have a body parser** — express middleware runs BEFORE every guard, so a route-scoped `express.raw` read up to 16 MiB of any body before throttle/sid/budget; `POST /box/media` decides the size from `Content-Length` and streams after the guard (`2026-09-23-metadata-pr11-box.md`).
- **The box media top rung is 32 MiB (owner 2026-09-24: raise now, not Phase 5), and nginx lets it through ONLY in `location /box/`** — that block is tracked in `infra/nginx/fireplace.conf` and LIVE on the VM since 2026-09-24 (backup `~/nginx-fireplace.bak.20260925T004016Z`); drop it and every `/box/` path falls to `location /` (POST 405, GET index.html). Never raise the vhost-level 21m for it. A 16.1–20 MiB file pads to 32 MiB: the 1 GiB/day budget is 32 top-rung files (`2026-09-24-box-32mib-rung.md`).
- **Server-side files outside `messages` go in their own `MEDIA_DIR/<dir>/`, never `msgs/`** — `MediaCleanupService` deletes every `msgs/` file no `messages` row references (`2026-09-23-metadata-pr11-box.md`).
- **`MediaCryptoService.encrypt` takes no caller key or IV** — so a box media retry BEFORE the upload landed reuses the ciphertext held in RAM (`_boxMediaBodies`); a restart loses it and the row stays failed. Never "re-encrypt for the retry": a new key orphans the id (`2026-09-26-metadata-item3-decision22.md`).
- **A box attachment's size cap applies to the UNFRAMED ciphertext** — a 16.1–20 MiB file downloads as the 32 MiB rung, and a cap on the download would refuse every such file (`2026-09-26-metadata-item3-decision22.md`).

## Deploy / CI / release
- **Check CI with `gh api repos/Lentach/Umbra/commits/master/check-runs`, never `gh run list --branch master`** — it returned weeks-old rows twice (`2026-09-08-session-dependabot-sweep-and-pc-health.md`).
- **`docker-compose.prod.yml` ENUMERATES every backend env var, so a new key in `~/fireplace/.env` never reaches the container** — `ANDROID_APK_*` was set correctly and `/version` still answered `"android": null`, which reads as working because `null` is also the right answer when nothing is published. Any new backend env var needs a line in the compose `environment:` block (with a `:-` default) or it is silently dropped (`2026-09-18-apk-update-check.md`).
- **NEVER host a downloadable file under `~/fireplace/frontend-build/`** — `deploy-web.ps1` publishes by `rm -rf frontend-build && mv ~/web-staging/web frontend-build`, so the next web deploy deletes it and any URL pointing there 404s. APKs live in the sibling `~/fireplace/apk/` with its own `location ^~ /apk/` (`2026-09-18-apk-update-check.md`).
- **knip runs with DEFAULT includes since 2026-09-19 (exports, types, duplicates too)** — a transitive package imported directly (`socket.io` was, 24 sites) reads as "unlisted dependency" and turns CI red: list it in `package.json`; a constant exported only for its own file is tolerated by `ignoreExportsUsedInFile`, a symbol nothing uses must go (`2026-09-19-refactor-dropped-d10-closed.md`).
- **A wire cut that refuses an old shape also breaks `e2e-wire` if the harness PINS that shape on the raw socket** — the D10 close turned CI red on `84d9608d` because `full_stack_e2e_test.dart` drove `addReaction {emoji:'🔥'}` deliberately; grep `test_e2e/` for the old shape in the same commit, never after the push (`2026-09-19-refactor-dropped-d10-closed.md`).
- **`deploy-web.ps1` overwrites `frontend/build/web` with the PROD bundle** — rebuild locally before any 8093 check (`2026-09-03-session-lxxii-reset-door.md`).
- `deploy-web.ps1` exits 1 in a worktree after `PUBLISHED_OK` — run the gate from the main checkout, never `-SkipVerify` (`2026-09-06-session-auth-clarity.md`).
- **Merging an older release branch must never roll the pubspec back** — prod served a branch as a test until the 0.2.0 deploy (`2026-09-01-session-video-messages.md`).
- "Merge it and deploy, I've tested it" may mean the BRANCH — confirm WHICH build was tested (`2026-09-05-session-video-merge-e2e-fix.md`).
- CI never runs on a plain branch push — only `master` pushes and pull requests (`2026-09-05-session-video-nits.md`).
- Deploy order is BACKEND FIRST whenever the change adds a wire field or a migration (D26 `0017`, D27 `platform`/`identityReplacedTo`, (lxxiii) opt-in lock).
- **Rollback hazards:** never serve a web build below `d446a9d` to a browser with the passcode ON; never below 0.2.23 to an account with a phrase backup; never below 0.2.26 (0.2.25 gates an install for the life of the process after one mistyped phrase). Tag `pre-multidevice-master` = `9b6ea1a`; migration `0015` is not code-reversible.
- CI lint ratchet is a release gate: hand-formatted backend edits fail the run AFTER tests pass — prettier only the files you touched (`2026-09-08-session-d27-rename-and-review.md`).
- **`prettier --check` warns on EVERY backend file in this working tree — it is CRLF, not bad formatting.** `core.autocrlf=true` checks out CRLF (`git ls-files --eol` → `i/lf w/crlf`) while prettier 3 defaults to `endOfLine: "lf"` and `backend/.prettierrc` (singleQuote + trailingComma only) does not override it; a prettier-CLEAN blob piped through `sed 's/$/\r/'` reproduces the warn exactly. Judge formatting from the COMMITTED blob (`git show HEAD:<path> > tmp.ts && npx --prefix backend prettier --check tmp.ts`), never the working copy, and never `--write` to silence it: git normalizes the bytes back on commit, so the only product is a whole-file line-ending churn window in a SHARED worktree (`2026-09-14-delete-irreversibility-proof.md`).
- **A `backend/package-lock.json` diff that is ONLY `"libc": ["glibc"|"musl"]` blocks is npm-version churn — `git restore` it, never commit it** — npm ≥11 writes that field on optional platform binaries, the npm 10.x bundled with Node 22 (what CI uses) strips it, so committing either direction makes the file ping-pong on every install. Measured 2026-09-14: 32 blocks / 97 lines removed, zero package, version or integrity changes. Check with `git diff backend/package-lock.json | grep "^-" | grep -v '"libc"\|glibc\|musl'` — empty output means it is pure churn (`2026-09-14-session-skills-mcp-audit.md`).
- **nginx serves the SPA `index.html` with HTTP 200 for unknown paths** — `/privacy` and `/.well-known/assetlinks.json` "exist" by status code while neither is a real file; check the BODY, never the status (`2026-09-13-session-android-release-research.md`).
- Actions billing/CI economics history: `2026-08-18-session-actions-billing-and-0.1.16.md`.

- **`Desktop/Fireplace` is on `feat/passcode-lock`, not master, and master is LOCKED in the `fireplace-0a` worktree** — root §1 claimed master until 2026-09-10 and the accuracy audit missed it (a git-state claim, not a code claim); push docs commits with `git push origin HEAD:master`, then push the branch too (`2026-09-10-session-workflow-2.0.md`).

- **CI Flutter is pinned through `frontend/pubspec.yaml` `environment.flutter:` (`flutter-version-file`)** — before 2026-09-10 CI floated on `channel: stable` (Dart 3.13) while the box ran 3.12, so a green CI could mean a different SDK than you tested. Bump the SDK by editing that one line; never re-float it (`2026-09-10-session-workflow-2.0.md`).
- **A `.java` file wakes CodeQL default-setup `Analyze (java-kotlin)` autobuild, which cannot build a Flutter module** — master went red on an unrelated check; default setup re-pinned to `["actions"]` via `PATCH …/code-scanning/default-setup` (`2026-09-12-session-batch-8-tooling.md`).
- **CI does NOT run on a feature-branch push, and a CONFLICTING PR gets no run either** — `ci.yml` triggers on `push: master` and `pull_request`, and GitHub skips `pull_request` workflows while it cannot build the merge commit, so a conflicted PR shows only CodeQL and reads as "CI never started". Resolve the conflict and push before judging CI silence (`2026-09-18-reaction-tokens-driven.md`).
- **`up -d --no-deps db` drops the backend pool** — `docker restart fireplace-backend-1` after any db recreate; backend image/`/version` stay untouched (`2026-09-12-session-batch-8-tooling.md`).
- **A superseded commit's checks read `cancelled`, NOT green** — GitHub kills in-flight jobs when a newer commit lands on the same ref, so an intermediate push can show one `success` beside four `cancelled` rows. Never call that green: verify the TIP with `gh api repos/Lentach/Umbra/commits/<sha>/check-runs` before any deploy or any "CI green" claim (`2026-09-14-session-delete-convergence.md`).
- **`deploy-web.ps1` runs an unconditional `flutter clean` — it can destroy a SIBLING session's `frontend/build/` artifacts** — there is no opt-out switch; on 2026-09-14 a 51 MB `app-release.apk` survived only because a Gradle daemon's file handles made the delete fail (`flutter clean` warns and continues either way). Copy the artifact out first, or build with `flutter build web` + `deploy-web.ps1 -SkipBuild` (`2026-09-14-e2e-gap-audit-and-deploy.md`).
- **A backend-only deploy makes `/version` and `/version.json` disagree** — `deploy-backend.sh` takes `APP_VERSION` from `frontend/pubspec.yaml`, so the backend reports the new semver while the served bundle keeps the old one. Cosmetic (no `minVersion` gate exists), but do not read `/version.json` as proof the backend deploy worked — use `/version`'s `gitCommit` (`2026-09-14-e2e-gap-audit-and-deploy.md`).
- **The VM's git remote still names the pre-rename repo (`Lentach/Fireplace.git`)** — `deploy-backend.sh`'s `git pull` works only through GitHub's rename redirect; `git fetch --dry-run` on the VM exits 0 today. Fix with `git remote set-url` before that redirect is ever withdrawn (`2026-09-14-e2e-gap-audit-and-deploy.md`).
- **`frontend/nginx.conf` is NOT what production serves** — it is the container nginx for the dev/staging composes. The live vhost is the host file, now tracked at `infra/nginx/fireplace.conf`; an audit that "fixes headers" in `frontend/nginx.conf` changes nothing users load (`2026-09-17-session-security-headers.md`).
- **Static headers belong ONLY in the two document blocks** — every proxied path already carries helmet's set from NestJS, so a server-level `add_header` emits a second, conflicting `X-Frame-Options`/CSP. `post-deploy-smoke.mjs` now asserts both halves (`2026-09-17-session-security-headers.md`).
- **CSP script hashes must be computed over LF, not the repo's CRLF** — the HTML parser normalizes newlines before hashing, so raw-byte digests match nothing; under enforcement that kills the pre-paint theme sync and the passcode curtain. `node scripts/verify-csp-inline-hashes.mjs` is the gate (`2026-09-17-session-security-headers.md`).
- **`/etc/nginx/sites-available/fireplace` was a STALE copy and `sites-enabled/fireplace` a real file, not a symlink** — the stale one still had `client_max_body_size 11m` (413s every 20 MB video), no `/contact`, no `no-cache`. Normalized to available+symlink 2026-09-17; if you ever find them diverged again, the LIVE one is whatever `sites-enabled` resolves to (`2026-09-17-session-security-headers.md`).
- **Under CanvasKit, `connect-src` — not `img-src` — governs every remote image** — `Image.network` XHRs the bytes and draws them, so Giphy thumbnails (`media0..4.giphy.com`) and `google_fonts` webfonts are connect-src fetches. A CSP that names only `api.giphy.com` + `font-src` looks right, reports 6+ violations, and enforced ships an EMPTY GIF picker (`2026-09-17-session-security-headers.md`).
- **Link previews on WEB are already same-origin** — `messaging_provider.send.dart:1445` routes them through `POST /messages/link-preview`; `LinkPreviewService.fetchPreview`'s direct `http.get` is the NON-web branch. Do not justify a wide `connect-src` with it — the only arbitrary-host need left is the og:`image` render (`2026-09-17-session-security-headers.md`).
- **A CSP pass means nothing until you are LOGGED IN, and it does not need prod** — recipe (own compose project + own ports + throwaway worktree + nginx mounting the real snippet) in `.omp/rules/production-vm-deploy.md`. Portrait viewport or the app eats every tap; read violations from the CDP `Log` domain and use a FRESH tab per run, because `Log.enable` replays the previous policy's entries (`2026-09-17-session-security-headers.md`).
- **Prod config changes are under the same green-CI gate as a deploy** — an nginx snippet is a production change; check `gh api …/commits/<sha>/check-runs` BEFORE `systemctl reload`, not after (own miss, 2026-09-17) (`2026-09-17-session-security-headers.md`).
- **PWA cold-start is a full multi-MB shell re-download, not a stall** — no offline SW cache (Flutter 3.44's generated `flutter_service_worker.js` self-unregisters on `activate`) + `infra/nginx/fireplace.conf` `no-cache` on `/`, so any idle-return / post-deploy launch re-fetches `main.dart.js` (7.6 MB) + local `canvaskit.wasm` (6.9 MB); daily users stay warm in the HTTP cache. `#fp-boot` in `web/index.html` MASKS the ~15 s blank, it does not shorten it — the real fix is gzip/brotli for JS/wasm + `immutable` cache on `/canvaskit/` (`2026-09-18-session-pwa-boot-loader.md`).
- **`scripts/verify-no-user-logs.mjs` is a CI gate: no backend string may carry an account id/username or an interpolated user field** — non-log literals (room names, Map keys, canonical wire JSON) opt out with `// log-guard: key` on the line; the guard self-tests before every scan (`2026-09-20-metadata-phase0-shipped-on-branch.md`).
- **A column write can double as an existence probe** — `devices.lastSeenAt`'s per-connect `update()` was also the row-exists check; dropping a column means re-reading the branch that used its affected count (`2026-09-20-metadata-phase0-shipped-on-branch.md`).
- **`key_bundles` upsert must stay `skipUpdateIfNoValuesChanged`** — without it the every-connect same-content re-upload re-stamps `updatedAt` = a per-device presence clock (`2026-09-20-metadata-phase0-shipped-on-branch.md`).
- **A bare `app.use(path, json({...}))` in a Nest bootstrap DISABLES Nest's own global JSON parser** — `ExpressAdapter.registerParserMiddleware` skips registration when a layer whose handler is named `jsonParser` is already on the stack, and that is exactly what `express.json()` returns, so every OTHER route silently loses its body. Wrap it in a NAMED function (`function contactBackupJsonParser(...)`). Proven with a negative control: bare mount → `/auth/login` accepted 1.5 MB and parsed nothing (`2026-09-20-metadata-pr24-pr23-review-fixes.md`).
- **`ci.yml` runs only on `push: master` and `pull_request`, so a branch push with NO open PR yields 0 check-runs** — indistinguishable from "CI is still queued", and `gh api .../check-runs` answers `total_count: 0` either way (confirmed after an 830 s poll). The trap is self-inflicting: a draft PR opened for CI is AUTO-MERGED the moment master fast-forwards past the branch, so it closes itself and every later push on that branch is unverified in silence — `gh pr list --state all` showed #181/#182/#183 all MERGED (2026-09-20/21), all head `feat/metadata-privacy`, which is why `12d9ec3d` has never been CI-verified. After any master fast-forward, re-open a draft PR before trusting a green history (`2026-09-22-metadata-logout-relogin-backup-freeze.md`).
- **`frontend/build/web` can hold a RIG bundle with a tunnel `BASE_URL` compiled in** — `app_config.dart:16` bakes it at build time, so publishing that directory ships a dead hostname to production and the SPA still boots from cache while every API call fails. Wipe the directory, rebuild via `deploy-web.ps1`, and `grep main.dart.js` for the tunnel host expecting 0 before any upload (`2026-09-22-metadata-logout-relogin-backup-freeze.md`).
- **`deploy-web.config.ps1` is gitignored and PER-CHECKOUT** — absent in `fireplace-mp`, so `deploy-web.ps1` builds and SILENTLY skips publishing (no target set). Copy it from a sibling worktree instead of asking the owner for VM details; likewise `config/env` for `GIPHY_API_KEY`, whose absence does not fail the build — it ships GIF search disabled (`2026-09-22-metadata-logout-relogin-backup-freeze.md`).
- **`validateDto` does NOT strip unknown fields — when a DTO shrinks, copy the kept fields explicitly** — `plainToInstance` keeps extras and `validateSync` runs without a whitelist, so an older client's removed field (`activeConversationId`) survives onto the instance; `handlePushClientState` now stores `{clientVisible}` alone, pinned by a spec that sends the old shape (`2026-09-22-metadata-lever-b-pr02.md`).

- **A host `npm install` prunes the dev container's Linux optional binaries from the shared `backend/node_modules`, and a container restart's npm 10 strips 96 `"libc"` lines from `package-lock.json`** — after either, `git diff --stat backend/package-lock.json`; if only libc lines moved, check the lock out (`2026-09-23-storage-loss-part-a.md`).
- **`@nestjs/throttler` must stay ≥ 6.7.0** — 6.5.0 kept one expiry-timer list per throttler NAME, so one tracker's lapsed block froze every other client's window (`2026-09-23-storage-loss-part-a.md`).
- **Two Socket.IO gateways on one port share ONE engine.io server built from the FIRST-scanned gateway's options** — a second gateway's `cors` is silently ignored; every gateway passes `common/socket-cors.ts` (`2026-09-23-metadata-pr11-box.md`).
- **The box's GLOBAL ceiling (60 000 blobs + 2 GiB media, decision 30) is a running total in the one-row `box_totals`, not a count — every path that stores or removes a box message or medium must move it in the SAME statement or transaction** — a missed decrement makes the box refuse `quota_exceeded` early and never recover; a missed increment lets it past ~3 GB on the disk Postgres shares. A test fixture that INSERTs `box_msgs` directly must bump `box_queues."msgCount"` and `box_totals` too (the int suite's final "running totals" test fails otherwise). Prod still pins `BOX_ENABLED: 'false'`: turning it on is the owner's call after G5 (`box.service.ts` header, `2026-09-23-metadata-g4-gate.md`).
- **Never pipe a plaintext prod `pg_dump` into staging** — the gpg passphrase that `staging.ps1 restore` asks for is the owner's consent gate on prod data; an additive migration is rehearsed on staging's own volume (`up` applies it at boot, then `sql` replays it) (`2026-09-23-metadata-g4-gate.md`).
- **`scripts/smoke/node_modules` is per-checkout, and `fireplace-mp` has no playwright** — the smoke's app-boot check fails with `Cannot find package 'playwright'`; run it from `fireplace` with `--commit <served web sha>` (`2026-09-23-metadata-g4-gate.md`).
- **`ConditionalModule.registerWhen(M, 'KEY')` treats an UNSET var as ENABLED** — `@nestjs/config` `conditional.module.js:38` is `env[key]?.toLowerCase() !== 'false'`, so a prod compose without the var would expose the module; always pass a `(env) => env.KEY === 'true'` predicate and prove "off" with the var UNSET (`2026-09-23-metadata-g4-gate.md`).
- **Since PR0.2, a VISIBLE app gets no OS push AND no sound for a message in a chat it is not viewing, on every client** — `messaging_provider.events.dart:139-141` returns before `_incomingSound.play()` (:174); owner signed off "badge only" at G4 and ruled again 2026-09-24: NO in-app cue (`2026-09-23-metadata-g4-gate.md`, `2026-09-24-metadata-pr31a-box-session.md`).
- **Adding or removing a Flutter test without bumping root `CLAUDE.md`'s count turns "Flutter analyze and tests" RED after every test passed** — the count gate runs last (`2616` vs documented `2613`); bump the Tests line in the same commit (`2026-09-25-metadata-e9-batch-per-token.md`).
## Tests / harness / falsification
- **If a mutant survives, the test is the thing that is wrong** — assert behaviour from the real gated state, never a diag marker (`2026-09-08-session-d27-rename-and-review.md`).
- **A harness can only find bugs in the shapes it builds** — the never-enrolled reset had a live population of exactly 0 (`2026-08-26-session-t10-reset-addressability.md`).
- **Never-enrolled is the DEFAULT population** — a probe can die on its own premise with its real assertions never running (`2026-09-05-session-video-merge-e2e-fix.md`).
- Never assume a red harness is the harness's fault: a red `e2e-wire` was three stale tests while a red `e2e-isolated-probes` was a real gap (lxxv) (`2026-09-05-session-video-merge-e2e-fix.md`).
- **`flutter test test_e2e` shares the 10/h/IP register throttle with any seed script — run the harness FIRST, seed after** — seeding 4 accounts first produced a 429 mid-harness and a 4-minute backend restart to clear it (`2026-09-20-metadata-pr22-contact-store.md`).
- **`Future.any([work, Future.delayed(budget)])` leaves the losing timer live for the whole budget** — every `testWidgets` that reaches that code fails with "A Timer is still pending"; race with a real `Timer` you cancel on completion (`ConnectionProvider._withinBudget`) (`2026-09-20-metadata-pr22-contact-store.md`).
- **`Future.delayed(Duration.zero)` does not flush a lock→snapshot→write chain** — expose the queue tail (`ContactStore.settled`) and await it; one event-loop turn is a flake generator (`2026-09-20-metadata-pr22-contact-store.md`).
- A mutation runner once left the mutant ON DISK — `git diff` after every mutation script (`2026-09-03-session-lxxii-reset-door.md`).
- A unit suite that pre-arms the engine cannot fail — the revoke button failed live with nothing in the log; drive the production path (`2026-08-21-session-t6-revocation.md`).
- `find.textContaining('dev')` also matches "device" — pin exact strings (`2026-09-09-session-c9-phrase-at-the-door.md`).
- **A wire client must wait for `socketReady`, not `connect`** — an emit sent in between is dropped with NO `error` event and no handler log, which reads as a silently broken feature; gate every scripted emit on `socketReady` and confirm with the backend's own DEBUG line (`2026-09-14-delete-irreversibility-proof.md`).
- A `str.count`/grep matcher must be CRLF-aware — F42 found 0 occurrences until it was; a CRLF-blind `sed` once missed the pubspec bump (`2026-09-09-…`, `2026-09-08-session-c8-…`).
- **Artillery socketio: the token can only reach `auth` as `{{ $processEnvironment.X }}`** — the launcher templates `config.socketio` before workers start (unresolved vars dropped) and the engine connects before the first flow step; `response` validates immediately only on an exact `data` match (`2026-09-12-session-batch-8-tooling.md`).
- **`flutter analyze` prints "N issues found" on stderr; Node ≥ 20 needs `shell: true` to spawn `flutter.bat`** — scrape both streams (`scripts/dart-lint-ratchet.mjs`) (`2026-09-12-session-batch-8-tooling.md`).
- **Take the Dart lint baseline on a clean worktree** — the main checkout flapped ±30 infos while a concurrent session edited `chat_input_bar.dart` (`2026-09-12-session-batch-8-tooling.md`).
- **Existing `testWidgets` device files are not Patrol tests** — under `patrol test` they hang in "Executing tests"; keep `flutter test integration_test -d` (`2026-09-12-session-batch-8-tooling.md`).
- **`auth.service.spec` recoverPassword second-boundary test flakes on slow runners** (equal second, expected greater) — rerun once; if it recurs, fake the clock (`2026-09-12-session-batch-8-tooling.md`).
- **Fixture rows in `chat_detail_identity_row_test.dart` are stamped `12:(id % 60)`** — i.e. 12:40–12:42; a test that reasons about their clock must use that, not "now" (`2026-09-12-session-c10-key-change-line-lifetime.md`).
- **`expect(mockServer.to)` / `expect(mockClient.emit)` each add an `unbound-method` error** — the backend lint ratchet refuses a raised floor (898), so assert room fan-out through LOCAL `jest.fn` doubles: capture the chained `.to(a).to(b)` rooms into an array and the payload into a local `emit` mock (`2026-09-14-session-delete-convergence.md`).
- **`e2eSql` rows include psql's command TAGS** — a multi-statement `-c` prints `CREATE TABLE` / `DO` on stdout even under `-At`, so a sweep that builds a temp table gets 1-field junk lines mixed into its results; filter by field count, never index blindly (`2026-09-14-session-e2e-encryption-proof.md`).
- **A negative sweep needs a positive CONTROL in the same test** — "the plaintext is in no column" proves nothing if the query is broken; assert the column count scanned AND that the same sweep finds a string that IS there (`2026-09-14-session-e2e-encryption-proof.md`).
- **Adding a suite to the shared `test_e2e` run turns an UNTOUCHED file red** — `/auth/register` is 10 per HOUR per IP with an in-memory counter every account in `test_e2e/` shares, and `ci.yml:292-297` records the default run as already spent to the edge. Merging `encryption_proof_test.dart` (2 registrations) failed `full_stack_e2e_test.dart`'s `setUpAll` with `ApiException(429)` on `f86a43de`, because the 429 lands on whichever suite runs LAST. The fix is a fresh backend (`e2e-isolated-probes`), never a raised cap; and the gate must be on the GROUP with `setUpAll`/`tearDownAll` INSIDE it, or the default run pays the registration cost just to skip (`2026-09-17-session-state-audit-cleanup.md`).
- **`addTearDown` inside `setUpAll` disposes your fixtures BEFORE test 1** — package:test runs `setUpAll` as its own synthetic test, so a callback registered there fires the moment setup completes. To keep a `tearDownAll` from throwing `LateInitializationError` over the real cause when setup dies early, guard it with a `var built = false` flag set after construction — do NOT make the `late` fixtures nullable (that ripples `!` through ~50 call sites) (`2026-09-17-session-state-audit-cleanup.md`).
- **`MediaCryptoService` cannot run under `flutter test` on the dev PC** — `dart run webcrypto:setup` dies with `CMAKE_C_COMPILER not set` (no MSVC), so VM-side AES-GCM must come from `pointycastle`; the app's real media path has to be evidenced on a device or in prod (`2026-09-14-session-e2e-encryption-proof.md`).
- **`POST /media/upload` needs a `mediaType` multipart FIELD** — a file-only body is a 400 `must be one of image, voice, gif, file, avatar, video`; media GETs are authenticated, anonymous is 401 (`2026-09-14-session-e2e-encryption-proof.md`).
- **`nest --watch` is INERT in the dockerised dev backend on Windows** — inotify does not cross the bind mount, so `touch src/main.ts` in the container changes nothing (same app PID across 6 probes). Resetting the in-memory register throttle (10/hr per IP) therefore costs a FULL `compose restart`, which re-runs `npm install`: **302 s measured** to a live `/health`. Budget it, or run the suite once and keep the whole transcript (`2026-09-14-session-e2e-encryption-proof.md`).
- **Do not call a slow boot a hang** — the same stack needs ~5 min from container start to `/health`, so a 30 s foreground run or a 3:37 observation window proves nothing about a backend that has not logged yet. Measure against the known-good boot time BEFORE recording a negative finding; one was nearly committed as fact this way (`2026-09-14-session-e2e-encryption-proof.md`).
- **A `ContentKv` test fake whose `noSuchMethod` returns null now BREAKS on `authoritativeSnapshot()`** — reaction-key reads went through the authoritative snapshot, so `await null` threw a TypeError that `loadReactionKey`'s `on Object catch` turned into `ReactionKeyUnavailable`; the test then "passed" its failure assertion while the pull it existed to check never ran. Implement the method in the fake (`2026-09-17-reaction-tokens-end-to-end.md`).
- **Bound Dart ratchet drift with a per-RULE diff between worktrees, never `file:line`** — every inserted line shifts the numbers below it, so a positional diff reports dozens of phantom "new" findings (`2026-09-17-reaction-tokens-end-to-end.md`).
- **A ratchet reading measured on a tree that fails to COMPILE is an artifact** — the analyzer emits errors instead of the file's usual infos, so the count drops; fix compilation, then re-measure before lowering the floor (`2026-09-17-reaction-tokens-end-to-end.md`).
- **`e2eSql` defaults to container `fireplace-db-1` — the DEV stack** — a harness run against staging/any other stack then READS AND WRITES the dev database while the stack under test never moves, and `registration_lock_test` fails with `existing`/`cooldown` mismatches that read as product bugs; export `E2E_DB_CONTAINER` (`staging.ps1 harness` now does) (`2026-09-18-reaction-tokens-driven.md`).
- **A test that still passes with its fix reverted defends nothing — delete it** — the decrypt-pass reaction clobber is unreachable from the provider harness (hydration consumes the RAM decryption cache one pass earlier), so its "regression test" was decoration; the browser A/B is the evidence instead (`2026-09-18-reaction-tokens-driven.md`).
- **`web_document_background_test.dart` pins only the FIRST `var accents` block** (`firstMatch`) — the `#fp-curtain` copy in index.html. The `#fp-boot` loader's duplicate accent map now lives in `web/fp_boot.js`, which that test does NOT read at all, so it can silently drift from `RpgTheme.ephemeralAccent`; keep it in sync with the curtain copy (`2026-09-18-session-pwa-boot-loader.md`).
- **The dockerised dev `nest --watch` DOES recompile on the Windows box (traps.md:80 is wrong here)** — never edit backend files while `flutter test test_e2e` runs; a recompile restarts the app mid-run and the failures look like flakes; two harness runs also exhaust the 10/h register bucket, and `docker compose restart backend` (~4 min) is the only reset (`2026-09-20-metadata-phase0-shipped-on-branch.md`).
- **Specs pin exact log text** — after any log-line change grep `*.spec.ts` for the old phrasing (`identity-reset.service.spec.ts`, `key-bundles.service.spec.ts` bit) (`2026-09-20-metadata-phase0-shipped-on-branch.md`).
- **A service called through `unawaited(...)` MUST translate primitive failures into its own taxonomy** — `webcrypto` is unavailable under `flutter test` on this box, so a PBKDF2 inside an unawaited login hook threw `UnsupportedError` into the zone and failed an UNRELATED e2e test (`auth_token_fault_injection`) with no hint of the cause (`2026-09-20-metadata-pr24-pr23-backups.md`).
- **`/auth/register` is 10/HOUR/IP and a full `flutter test test_e2e` run empties the bucket** — a suite re-run, or any curl/seed after it, then fails with `register` returning false, which reads as a product regression. Only `docker compose restart backend` resets it (~3 min); run the ONE file you are debugging FIRST on a fresh bucket before calling anything a regression (`2026-09-20-metadata-pr24-pr23-backups.md`).
- **A server-side byte compare can never detect an unchanged encrypted payload** — AES-GCM draws a fresh IV per seal, so re-sealing an identical graph is different bytes every time and any `existing.blob === dto.blob` guard is structurally dead. Decide the no-op on the PLAINTEXT client-side, or the row's `updatedAt` becomes the per-account presence clock the whole series removes (`2026-09-20-metadata-pr24-pr23-review-fixes.md`).
- **AES-GCM is length-preserving, so an unpadded blob's SIZE is a record counter** — a `pg_dump` with no key estimates the contact count from `length(blob)`, and two dumps show the graph growing. Pad the plaintext to fixed buckets (`uint32be(len) || body || zeros`) before sealing, and never write "no count is visible" next to an unpadded column (`2026-09-20-metadata-pr24-pr23-review-fixes.md`).
- **A 409 re-read must MERGE, not adopt the server's state wholesale** — the contact-backup retry replaced the locally staged wrap set with the server's, so a password change racing another upload reported success having published the OLD wraps, and the change then destroyed the only password that opened them. Any optimistic-concurrency retry that carries new state must re-apply it on top of the re-read (`2026-09-20-metadata-pr24-pr23-review-fixes.md`).
- **A test that calls `uploadNow()` (or any flush) BY HAND cannot see a dead notification path** — the production symptom was `store.onChanged` nulled by `clear()`, so nothing ever scheduled an upload; the hand call passes on a store that notifies nobody. Assert through the real `onChanged` path (`store.update` → `settled` → `pumpEventQueue`) and check the backend's PUT count. This shortcut is what hid G2's B1 (`2026-09-22-metadata-logout-relogin-backup-freeze.md`).
- **`ConnectionProvider.connect()` swallows a second call inside the reconnect cooldown (`:310-319`), so a test driving two sessions proves nothing** — the first RED for the logout→login freeze was red only because connect never ran. Pass `immediate: true` for a user-initiated login, and re-establish the red for the RIGHT reason before trusting any diagnosis built on it (`2026-09-22-metadata-logout-relogin-backup-freeze.md`).
- **Docker Desktop writes BOTH an A and an AAAA record for `host.docker.internal`, and `getent` returns the IPv6 ULA FIRST** — nginx round-robins onto the unroutable address and answers 500 ITSELF, intermittently, so fresh-connection probes pass while the real client fails. Pin the upstream to a compose-network service name (IPv4-only embedded DNS); `resolver ... ipv6=off` does nothing for a statically resolved name (`2026-09-22-metadata-logout-relogin-backup-freeze.md`).
- **A `flutter test` job killed by the bash tool's deadline leaves `flutter_tester.exe` orphans and a log frozen at its last `+N` stamp** — a stamp that stops advancing is a DEAD run, not a slow one (20 min were spent waiting on `+1255`); run the full suite as a `hub start` process and `hub wait for=exit`, and kill only `flutter_tester.exe` — a loose `dart.exe` filter also kills the lint ratchet's analyzer and the MCP/LSP servers (`2026-09-22-metadata-run-c-close-2a.md`).
- **A red-first run that starts compiling after the fix landed is not red** — `flutter test` reads sources ~30 s in, so an edit applied while the "red" job compiles yields a green that proves nothing; prove red by parking the lib diff (`git diff > patch && git checkout -- file`, run, `git apply patch`) (`2026-09-22-metadata-run-c-close-2a.md`).
- **The Dart ratchet fails on +1 info, and new TEST code is where it comes from** — an escaped apostrophe (`avoid_escaping_inner_quotes`), un-cascaded `setUp` calls, an un-awaited `onMessageHistory`, and `this.` before an extension member (Dart resolves it unqualified — `unnecessary_this`). Map the analyzer's lines onto `git diff -U0` hunks instead of guessing (`2026-09-22-metadata-lever-b-pr02.md`).
- **`npx jest <spec>` in `backend/` refuses to run ("Multiple configurations found")** — `jest.config.json` and a `jest` key in `package.json` both exist; pass `--config jest.config.json` (`npm test` already does) (`2026-09-22-metadata-lever-b-pr02.md`).

- **Verify one part on a CLEAN worktree while builders share the tree** — `git worktree add --detach` + apply only that part's diff; in-flight sibling edits broke compiles and would pollute the CLAUDE.md counts (`2026-09-23-storage-loss-part-a.md`).
- **`expect.any()` is typed `any`: each use in a spec adds a `no-unsafe-assignment` error to the ESLint ratchet** — assert concrete shapes (id length regex, exact key list) instead (`2026-09-23-metadata-pr11-box.md`).
- **`npm run test:int` SKIPS without `BOX_IT_DATABASE_URL` ("Tests: 0 total") and FAILS in CI** — locally use `postgres://postgres:postgres@localhost:5433/postgres`; a red test holding an open HTTP request hangs teardown, so bound such waits (`2026-09-23-metadata-pr11-box.md`).
- **Box SQL lock order: `box_queues` rows (rid order) BEFORE `box_msgs` rows, and the one `box_totals` row LAST** — ack/sweep taking the msg first deadlocked against `enqueue`'s eviction (40P01, 14 of 50 `internal`); `box_totals` is every send's and upload's hot row, so after taking it a transaction may only write rows it already holds or new ones (`2026-09-23-metadata-pr11-box.md`, item 9).
- **Dart's `base64Url.decode` already refuses non-zero spare bits** — a re-encode compare after it is dead code (its mutant survived); pin the property with a test, not a second check (`2026-09-23-metadata-pr12-box-client.md`).
- **A killed eval kernel never runs `finally`: the mutant stays on disk** — a red int-suite test held a socket open, the kernel was killed, `box.gateway.ts` kept the swap; run each mutant as a subprocess with a `timeout`, then `git status` (`2026-09-23-metadata-pr12-box-client.md`).
- **`flutter test --platform chrome` sat at `loading` for 14+ min on this dev box (2026-09-23), cause unknown** — for a web-only number, `dart compile js -O4` a throwaway entrypoint, serve it and read `console` in the managed browser (the `verify-session-lock-probe.mjs` pattern); that ran in seconds (`2026-09-23-metadata-pr13-box-roundtrip.md`).
- **Yielding per task is not enough if all the yields are queued at once** — 256 zero-delay timers started together (`Future.wait`) let FRAMES through (0 long tasks, rAF gap ≤ 50 ms) but ran ahead of every later Dart timer for the whole 2.5 s burst. Chain them so only one turn is pending; measure Long Tasks, rAF gaps AND a timer scheduled mid-burst (`2026-09-23-metadata-pr13-box-roundtrip.md`).
- **In `fireplace-mp`, `test_e2e`'s two `registration_lock_test` failures are the CONTAINER default, not a bug** — `e2eSql` targets `fireplace-db-1`, this worktree's DB is `fireplace-mp-db-1`; the G4 entry logged them as "predating today". Run with `E2E_DB_CONTAINER=fireplace-mp-db-1` → 4/4, harness 46/16 (`2026-09-24-metadata-pr21-wire-id.md`).
- **`set VAR=x&& cmd` in the agent's bash tool does NOT export the variable** — `E2E_DB_CONTAINER` stayed unset, `e2eSql` read `fireplace-db-1` (the OTHER worktree's DB) and a correct new test failed; use `VAR=x cmd` or pass `env=` to a subprocess (`2026-09-24-metadata-pr32-first-contact.md`).
- **`devices` carries an FK-name drift from 0015 (`devices_userId_fkey` vs TypeORM's generated name)** — a schema-builder "entity == migration" check on that table must be scoped to the columns the migration owns, or it fails on a pre-existing rename (`2026-09-24-metadata-pr32-first-contact.md`).
- **The `PrefsContentKv` mock commits writes synchronously, so a two-context read-then-write race test on it PASSES without any lock** — wrap the kv so writes commit a turn later (`_SlowCommitKv` in `contact_store_inbox_test.dart`) and prove the test RED with a pass-through lock before trusting it green (`2026-09-24-metadata-pr31b-box-dispatcher.md`).
- **A mutation run's `git diff --stat` cannot show a stuck mutant in a file that already carries your edits** — hash each target before the run and compare after (`2026-09-24-metadata-pr31b-box-dispatcher.md`).
- **Python `read_text`/`write_text` on Windows rewrites LF as CRLF** — a mutant harness restoring with `write_text` flips every file's endings (whole-file churn, a failed hash check); snapshot and restore with `read_bytes`/`write_bytes` (`2026-09-24-metadata-pr31b-box-dispatcher.md`).
- **One extra `await` before the old-path emit in `_encryptAndSend` fails `chat_input_bar_attachment_test` ("image emits before caption")** — the test sends inside `tester.runAsync`, so a real-zone microtask deferred past it never runs and the caption's TEXT emit vanishes; take anything synchronous (the box's `addressesFor`) before the first await (`2026-09-24-metadata-pr31c-box-send.md`).
- **A test harness that re-implements the provider's reaction tests the copy** — `box_session_siblings_test`'s `_Device.read` did store→ack by hand; the reply handoff then lived only in the provider. Keep reactions in the box layer (`BoxSession.takeSiblingHandoff`) so the two-device test runs shipped code (`2026-09-25-metadata-sibling-queues-a.md`).
- **Flutter web send button: the first click after CDP `Input.insertText` only shows the `Wyślij` tooltip** — click again, and read the diag (`BOX_SEND`) before calling a send lost (`2026-09-25-metadata-sibling-queues-b.md`).
- **Web `SharedPreferences` over CDP: key `flutter.<KEY>`, String value JSON-encoded TWICE** — read `JSON.parse(JSON.parse(localStorage['flutter.K']))`, write `JSON.stringify(JSON.stringify(v))`; a bare key is never seen (`2026-09-25-metadata-sibling-queues-b.md`).
- **A box int test that ends with a socket holding an unacked message leaks a wake-up push into the NEXT test** — the suite's disconnect schedules it 2.5 s later; ack before the test ends (`2026-09-25-metadata-e9-box-push.md`).
- **A real-millisecond timer in a unit test flakes on the CI runner in BOTH directions** — a 40 ms `retryAfter` fired before the first assert (green 5/5 locally); keep ≥0.5 s between any assert and the timer it races (`2026-09-25-metadata-e9-box-push.md`).
- **Two helpers in one worktree: a mutant run rewrites lib files under the other's tests** — a false red sends one chasing a phantom bug, a false green hides a real one. Run mutants in a scratch `git worktree add --detach` copy, and re-run the combined set yourself on the final tree (`2026-09-26-metadata-item3-decision22.md`).
- **`future.whenComplete(() => map.remove(k))` returning the attached future made it wait on itself** — the first fetcher green run hung; drop in-flight entries with try/finally (`2026-09-26-metadata-item3-decision22.md`).
- **An optimistic-edit race test must keep the NEWER edit in flight while the older one settles** — with the newer already settled, the pending-edit ownership guard's mutant survived, because nothing was left to wrongly revert (`2026-09-26-metadata-item4-item9.md`).
- **A future made in the real zone never completes inside `fakeAsync`** — a box retry-timer test whose provider was built in `setUp` stalled at the first await (the row never went); rebuild the whole setup inside the `fakeAsync` body and drive it with `elapse` (item 4 gaps, E19l in `docs/plans/metadata-privacy-decisions.md`).
## E2E / multi-device / recovery
- **Sideloaded users SKIP versions, so drift migrations must be cumulative (1→2, 2→3, …), never "from the previous release"** — an APK is a whole app, not a patch, so a phone on `20049` can install `20052` directly and its content DB jumps three schema steps at once. `content_db.dart` is still `schemaVersion => 1` with `onCreate` only, so nothing steps today; the first bump to 2 is where a "previous version only" migration would corrupt a skipping device's SQLCipher store, which holds decrypted content keys and has no server copy (`2026-09-18-apk-update-check.md`).
- Registration lock is OPT-IN — arms only once `account_authorizations` has a row; a client that sees no `linkingEnabled` field fails CLOSED to the old gate (`2026-09-05-session-optin-lock-gate.md`).
- **`runSessionCrossContextLocked` is a PASS-THROUGH off web** (`session_cross_context_lock_stub.dart`) — any read-modify-write that relies on it alone has no serialization on Android or under `flutter test`; chain an in-process Future queue inside it (`2026-09-20-metadata-pr22-contact-store.md`).
- **`restoreAfterPasscodeUnlock` returned early on `_e2eInitialized`, so anything chained after `initializeE2E` there never ran when E2E came back by another route** — a mirror of a revoke-side teardown must run outside that guard (`2026-09-20-metadata-pr22-contact-store.md`).
- **A server list omitting a peer is INFERRED absence, not an event** — PR2.2 removes friend records the non-empty `friendsList` omits; the moment a record carries irreplaceable material (Phase 1 queue keys) the sweep must DOWNGRADE, not delete (owed in PR1.2) (`2026-09-20-metadata-pr22-contact-store.md`).
- **`message_envelopes.deliveredAt` is stamped ONLY by the explicit `messageDelivered` ack** (`chat-message.service.ts:798-803`) — NULL on rows a peer read straight from an open chat is normal, not a delivery bug (`2026-09-20-metadata-pr22-contact-store.md`).
- A `registrationId` may not change under an unchanged identity; the OTP replenishment path bypasses `upsertKeyBundle` so BOTH uploads carry the (lxiv) guard; residual `_reenrollAfterReset` has no in-flight latch — recorded, not fixed (`2026-08-30-session-bounded-gate-lxiv.md`).
- A warning the user cannot act on is a dead end; recovery state must survive a restart — read the THREE addenda (`2026-08-29-session-d1-d2-recovery.md`).
- A term added to a gate predicate needs an owner that clears it — read `clearAll()` when adding state to a process singleton (`2026-09-08-session-c7-final-review.md`).
- Login hardcoding `deviceId = 1` locked owners out after a reset — login resolves the LIVE PRIMARY; session gates deny only on an EXPLICIT `revokedAt`; `getServedMessageIds` answers a revoked device with SILENCE (`2026-08-21-session-t6-revocation.md`).
- Hostile `nextListVersion` inflation is bounded by a plausibility ceiling (`2026-08-26-session-t10-reset-addressability.md`).
- Wake delivers `hidden → inactive → resumed`; the privacy curtain is the DOM `#fp-curtain`, not a Flutter widget (`2026-09-06-session-passcode-wake-curtain-merge.md`).
- Multi-device permanent record: `.planning/multi-device/` (`FINISH-HERE.md`, `progress.md`, `task_plan.md`) and `docs/design/multi-device.md` §12 — not LATEST.
- **SETTLED 2026-09-13: messages sent AFTER a peer wipes/reinstalls are expected damage, not a receiver bug** — the backlog is gone for good, but the peer IS asked to re-key (`notifyPeer: true` in every observed row) and new messages decrypt once they confirm the new safety number; verdict, evidence and the ruled-out fixes are in `docs/runbooks/e2e-decryption-failed.md` Step 3G (`2026-09-13-session-post-wipe-fork-settled.md`).
- **`hadIdentityReset` is the unpersisted in-memory `needsKeyUpload` (`encryption_provider.dart:119`) — but the bundle upload does NOT clear it** (`:1469` only reads it; clear sites are `:1473`, `:4296`, `:4336`), so it stays TRUE for the whole minting process and the ordinary reinstall path DOES take the `identityReset` branch. `idReset:false` means a process that did not itself mint (FCM-woken). The earlier "can never take that branch" reading was wrong (`2026-09-13-session-post-wipe-fork-settled.md`).
- **A keyless login RE-MINTS on an UN-ENROLLED account and GATES on an enrolled one** — `encryption_service.dart:1166` throws `E2eIdentityIncompleteException` when `guard.exists && guard.linkingEnabled`; only `guard.exists` WITHOUT `linkingEnabled` reaches `IDENTITY_GUARD_UNLOCKED_REMINT` / `IDENTITY_MINTED {reason: server-bundle-unlocked-remint}` (`:1201-1208`). It is the identity-bootstrap guard, NOT a property of the door you logged in through — observed 2026-09-13 via "Nie pamiętam hasła" on an un-enrolled account, which is why that door looked like the cause (`2026-09-13-session-apk-drill-rerun.md`).
- **The durable E2E diag panel is reached by LONG-PRESSING the shield on Privacy & Safety** (`🔓 hacker mode`) — grouped failures + `DECRYPT_DECISION` rows that survive restarts; there is no other way to read `e2e_diag_persist_v1` on a release build (no root on the emulator's production image) (`2026-09-13-session-apk-drill-rerun.md`).
- `recordPeerIdentityChangedFromServer` needs a pinned anchor — call `buildSession` FIRST (`2026-09-12-session-c10-key-change-line-lifetime.md`).
- **A peer's account-identity anchor NEVER auto-advances from a server event** — `_demoteKeyChangeIfMuted` can only promote a candidate the device recorded (`encryption_service.dart:306-319`), so after your re-mint their FIRST send is refused with `AccountIdentityMismatch` and a red pill until a human confirms. By design (xlvi) — do not "fix" it (`2026-09-13-session-post-wipe-fork-settled.md`). Since 0.2.43 the pill no longer waits for that refusal: it renders for any peer in `peersWithChangedIdentity` whatever the key-change warnings setting says (`2026-09-13-session-unreadable-row-copy.md`).
- **Sending first does NOT spare the peer that confirmation** — device-proven: the peer decrypted the wiped device's PreKey message and its reply still bounced `AccountIdentityMismatch`; the outbound path validates the anchor regardless of an existing session (`2026-09-13-session-post-wipe-fork-settled.md`).
- **A recovery phrase is INERT on an un-enrolled account** — with 12 words saved but linking off, `pm clear` → log in re-mints silently and never offers the restore door; the door lives on the device-link gate. And `Włącz łączenie` forces REGENERATING the phrase even when one exists (`2026-09-13-session-post-wipe-fork-settled.md`).
- **A phrase restore REBINDS the device id** — a peer app already open keeps sending to the old id (`Bad state: Recipient has no key bundle (… deviceId=1)`) and shows the restored device's first message as `[encrypted]`; ONE reload of the peer app clears both (`2026-09-13-session-post-wipe-fork-settled.md`).
- **`hadIdentityReset` is process-wide and peer-AGNOSTIC** — during the whole life of a minting process it is true for EVERY peer, so an UNRELATED peer's transient `noSession`/`unknown` takes the `identityReset` branch: `markContentFailed: true`, `retryAction: none`, plus a `requestSessionRebuild` that peer never needed. Bounded (`persist: false`, so a later launch can recover the row) and not field-observed, but it is the strongest red-first test candidate in this area (`2026-09-13-session-post-wipe-fork-settled.md`).
- **The key-change pill is no longer gated on `keyChangeWarnings` (0.2.43)** — a peer in `peersWithChangedIdentity` means the anchor never advanced, which means every send to them fails closed, so the door must be visible BEFORE the user composes; the muted (lxxix) note is suppressed whenever the pill shows (`peerKeyChangeNoteAt` is computed under `!peerIdentityChanged`). Do not re-gate it on the setting — OWNER-APPROVED 2026-09-13, (lxxix) narrowed to absorbed changes only (`2026-09-13-session-unreadable-row-copy.md`).
- **`[encrypted]` / `[Decryption failed]` are internal sentinels and must never render (0.2.43)** — relabel in `sentinelDisplayText` (`utils/message_display_text.dart`) ONLY: TWO surfaces draw a message body (the bubble's `TextMessageContent._displayBody` and `messageDisplayContent`, used by `ChatMessageBubble` and the provider-free long-press replica), they both delegate to it, and when they decided separately they disagreed — the replica localized `[Decryption failed]` while the bubble printed it raw. Display-only, never rewriting `MessageModel.content` (the failed label is persisted and the durable cache must keep seeing it). An OWN `[encrypted]` row needs the LITERAL, not `displayAsEncryptedPlaceholder`: a fan-out row carries no ciphertext for its own origin device (ix), so the predicate misses it. And resolve `AppLocalizations` lazily per branch — hoisting it crashed `bubble_redesign_test.dart`, which pumps a plain bubble with no localizations ancestor, once in the bubble and again in the helper (`2026-09-13-session-unreadable-row-copy.md`).
- **"Relabel in `sentinelDisplayText` ONLY" means the message BODY** — `utils/reply_preview_helper.dart` is a THIRD surface that matches the same literals by hand and answers `l10n.encryptedMessage` for a quote, deliberately (a quote is one line, it tries `getCachedDecryption` first, and it takes plain `String` labels so the provider can call it without a `BuildContext`). Same row therefore reads "nie można odczytać" in its bubble and "Wiadomość zaszyfrowana" when quoted — intended, do not "unify" it (`2026-09-13-session-unreadable-row-copy.md`).
- **`_markMessageFailed`'s `errorMsg` argument goes NOWHERE** — ~15 call sites in `messaging_provider.send.dart` compute a reason ("Image too large (max 20 MB)", the whole `_userFriendlySendError` ladder) and the method stores only `deliveryStatus: failed`; `markSendingMessagesFailed` drops its argument too. Do NOT "wire them up": they are unlocalized English and would ship into a Polish UI. The one reason a user needs is rendered instead from a per-ROW verdict (`MessagingProvider.sendRefusedForIdentity(tempId)`, written from the thrown `AccountIdentityMismatch`) AND the peer-level `peersRefusedIdentity` — the peer set ALONE blamed a key change for a timeout in the same chat. Localizing the other ~14 is an owner copy decision (`2026-09-13-session-unreadable-row-copy.md`).
- **A disappearing message does NOT count down from SEND** — the row is created with `expiresAt: null` + `disappearAfterSeconds` (`chat-message.service.ts:346, 446`) and `expiresAt = now + ttl` is stamped only when the recipient READS it (`messages.service.ts:811-813`), so an unread 60 s row sits at `DELIVERED` indefinitely; the only floor is the never-read backstop of **1 day** (`DISAPPEARING_MAX_UNREAD_SECONDS`, `disappearing.constants.ts:8`). Expired rows are then HARD-removed with media unlinked first by an `EVERY_MINUTE` cron (`message-cleanup.service.ts:39-99`) — a real delete, not a display filter. **Never report "it didn't disappear" without reading `expiresAt` first**: this nearly became a phantom defect on 2026-09-14 (`2026-09-14-session-apk-field-test-list-preview.md`).
- **Emitting a delete to a SOCKET instead of a room is a privacy bug, not a refresh nit** — `messageDeleted` / `chatHistoryCleared` told the acting socket plus the peer's room, so the deleter's OWN other devices kept the decrypted plaintext while the row, its envelopes (FK CASCADE) and its media were already destroyed. `for_me` is the deliberate exception: it fans to the hider's room ONLY, because `hiddenByUserIds` is per-user and the peer must keep reading the message (`2026-09-14-session-delete-convergence.md`).
- **Distribution is a PLAIN CLOUD LINK, never a GitHub Release — owner's decision 2026-09-14.** A release on `Lentach/Umbra` (public) would be world-downloadable while REGISTRATION against prod is open, and "friends" is not "anyone who finds the repo". A `v0.2.48` draft was created and DELETED; `gh release list` is empty and no `v*` tag exists, which also keeps the Play App Signing PEPK door open. Consequence to budget for: **no auto-update path at all** — Obtainium needs a release feed, so every update is hand-delivered. Re-opening this means option (d), a server-side invite gate on registration (`2026-09-14-session-apk-weight.md`).
- **Durable delete convergence ALREADY EXISTS — do NOT build tombstones.** Room emits reach only CONNECTED sockets (socket.io buffers nothing for the rest), and the answer is the client PULL: `getServedMessageIds` → `reconcileStoredPlaintext` purges every stored id the server no longer serves, at each `socketReady` behind a 6 h stamp (`frontend/docs/e2e-invariants.md`, four load-bearing properties). A server-side ledger of what was deleted and when is a privacy liability and was rejected for that reason after being built and reverted. The reconcile must also EVICT THE ROW, not only its plaintext — a long-lived session offline at delete time still holds it in memory; a COLD start was always fine (`2026-09-14-session-delete-convergence.md`).
- **Authorization must precede the media unlink, not follow it** — `handleDeleteMessage` gated only on conversation MEMBERSHIP and unlinked the file BEFORE `deleteById` checked the sender, so a RECIPIENT emitting `deleteMessage {mode:'for_everyone'}` destroyed the sender's blob on disk, was refused the row delete, and left a message whose `mediaUrl` 404s forever. Any new destructive path: authorize first, media second, row third (`2026-09-14-delete-irreversibility-proof.md`).
- **The empty-chat branch needs its OWN top clearance** — the reverse message list anchors at the bottom so `padding.top + 8` is enough there, but the empty-state Column is static: without `+ GlassTopBar.capsuleHeight + 16` the `PeerIdentityChangedRow` — the only takeover warning in the product — paints UNDER the glass capsule and is unreadable (`2026-09-14-delete-irreversibility-proof.md`).
- **Before claiming the E2E story is strong, read `docs/audit/2026-09-14-e2e-gap-audit.md`** — local and untracked by design (`.gitignore:85`). It separates what is PROVEN (deletion irreversibility, client-side AES-256-GCM media, key-change blocking sends, the DAK chain closing ghost devices) from what is merely inferred and from what nobody has checked; it also defers to `docs/audit/2026-07-07-metadata-privacy-audit.md` on all metadata rather than re-inventorying it. Never copy its contents into a tracked file (`2026-09-14-e2e-gap-audit-and-deploy.md`).
- **Treat the `handleDeleteMessage` ordering defect as a CLASS, not a one-off** — every destructive path must authorize first, then media, then row. When touching any of them, audit the ordering rather than assuming it was done (`2026-09-14-e2e-gap-audit-and-deploy.md`).
- **The server does NOT refuse plaintext — E2E is CLIENT-enforced** — `chat-message.service.ts:443` stores whatever `content` arrives, so a raw `sendMessage` emit lands readable (dev row 977 `DELPROOF-RECIPIENT-UNLINK`); the fleet-level guarantee is empirical (230/230 prod rows `[encrypted]`, 2026-05-17→09-14), never structural (`2026-09-14-session-e2e-encryption-proof.md`).
- **`conversations` keys are `user_one_id`/`user_two_id`, `messages` uses `sender_id`/`conversation_id`** — the FK columns are snake_case while everything around them is camelCase; guessing `senderId` in raw SQL errors out (`2026-09-14-session-e2e-encryption-proof.md`).
- **A `keyBundleUploaded` handler can run its work TWICE — the owed-enrollment offer rides every authenticated upload and fires unawaited** — two `_reenrollAfterReset` passes inside the 20 s ack window each minted a DAK and then destroyed each other's ack channel, so neither promoted. Latch before the mint, never at the ack ((lxxxv), `2026-09-17-session-security-headers.md`).
- **Reaction-key reads MUST use `_authoritativeSnapshot()`, like every other durable record** — absence is the verdict that spends the one-shot Signal mailbox row (or mints at epoch 0), so a stale per-engine cache does not miss the key, it DESTROYS it: the 2026-07-29 mechanism with no second copy (`2026-09-17-reaction-tokens-end-to-end.md`).
- **Silence from `uploadReactionKey` is NOT a refusal** — the server may have accepted and advanced the epoch with only the ack lost, so deleting the local key there is unrecoverable (the epoch rule then forbids re-keying). Keep it marked `pending` and reconcile against the server's epoch; drop only on an explicit refusal (`2026-09-17-reaction-tokens-end-to-end.md`).
- **Never reuse the send path's device-1 fallback for key distribution** — a send survives a bad guess because the server refuses a stale send; `uploadReactionKey` has no such gate, so a missed live device can never obtain that epoch's key. Resolve both device lists and fail closed (`2026-09-17-reaction-tokens-end-to-end.md`).
- **A newly linked device shows placeholder reaction chips FOREVER** — same-epoch top-up and revoke rotation are server-supported but no client sends them (owner ruling: out of scope). It is not waiting for a mechanism that exists (`2026-09-17-reaction-tokens-end-to-end.md`).
- **A resolved reaction map must survive EVERY post-install merge, not just the entry point** — reactions resolve where rows ENTER, but a cold history entry awaits plaintext hydration (so the local-key acquisition lands before the rows reach `_messages`) and the decrypt pass then merges the RAM decryption cache, whose `server.copyWith(...)` reactions win; both reverted named chips to the unreadable placeholder until a relaunch drive caught it (`2026-09-18-reaction-tokens-driven.md`).
- **`_legacyDecryptedContentFallback` is a LIVE Android read path, not pre-sealing dead code** — `getDecryptedContent`/`recordExists` fall through to `flutter_secure_storage` on mobile (`encryption_service.dart:2922, :2950`); a false miss re-decrypts a consumed ratchet message into a terminal `[Decryption failed]`, and the VM suite's mock store cannot catch its removal. Prove the phone's namespace empty first (`2026-09-19-metadata-research-and-refactor-plan.md`).
- **`deviceId == 1` is spec §8 policy (`DEFAULT_DEVICE_ID`), not multi-device migration residue** — gateway rooms, key-exchange exemptions, JWT claim and the row's session owner all key off it and specs pin it; an "audit the compat fallbacks" task here produces nothing (`2026-09-19-metadata-research-and-refactor-plan.md`).
- **Reaction-key (J) and terminal-dup (I) code are NOT `ContentKv`-only tenants** — J reads through `_rawRecord(await _authoritativeSnapshot(), …)` (`:500-504`), I calls `_reloadPrefsForCrossContext` before RMW (`:3578, :3603`); an extraction that takes only `prefs` compiles, passes the fake-backed test, and reintroduces the key-destroying stale read (`2026-09-19-metadata-research-and-refactor-plan.md`).
- **Reactions accept the 22-char token ONLY since 2026-09-19 (D10 closed)** — a client below 0.2.49 gets `error { message }` on `addReaction`/`removeReaction`; rows written during the compat window may still hold a plaintext key and stay readable/removable (`2026-09-19-refactor-dropped-d10-closed.md`).
- **`pm clear` NEVER shows the storage-loss screen** — it wipes the key AND the file, and `db-recreate` needs a surviving `fp_content.db` with a freshly minted key; the loss surface is driven by `run-as com.fireplace.app rm shared_prefs/FlutterSecureStorage.xml shared_prefs/FlutterSecureKeyStorage.xml` (the Keystore-backed JWT dies too, so it starts at login) (`2026-09-22-metadata-run-c-close-2a.md`).
- **A nudge-only phrase gives no restore door** — `Zabezpiecz konto — utwórz 12 słów` enrols `recovery_keys` without `account_authorizations`, and the gate opens only on `exists && linkingEnabled` (`encryption_service.dart:1344`), so "keys match after a loss" needs `Włącz łączenie`, not just a phrase (`2026-09-22-metadata-run-c-close-2a.md`).
- **A content-based early return must still spend any one-shot armed for the same report** — the (lxxx) own-key branch returned before `_ownPublishUnacknowledged` was consumed, so after every re-mint or §6.2 ceremony the armed flag silently ate the next FOREIGN replacement at reconnect (an offline device's only alarm path) until (lxxxvii) (`2026-09-22-metadata-lever-b-pr02.md`).
- **After a re-mint, a thread that STARTS at the "Wcześniejsze wiadomości nie są dostępne na tym urządzeniu" divider is by design ((lxxxvi))** — unreadable rows the server stamped before `EncryptionService.ownIdentitySince` (the audit instant that ended at our key) are hidden like pre-link rows; they still exist, still go through the decrypt pass, and a readable one (history import) is never hidden (`2026-09-22-metadata-lever-b-pr02.md`).
- **A wiped linking-OFF install shows the red "Nowe klucze szyfrowania na Twoim koncie" for its OWN earlier identity change — NOT fixed** — only when the account already has an `identity_change_audit` row (its second storage-loss re-mint onward, or after any earlier re-mint/§6.2 reset): the first connect-time status arrives before any local identity exists, so that older row falls through as foreign, and the new own-key row is a different instant, so the (lxxxi) clause-2 retraction never touches it. An account whose identity never changed has no row and sees no banner (`2026-09-22-metadata-lever-b-pr02.md`).
- **After a re-mint, EVERY cold start re-raised the red own-key banner about the install's OWN row — FIXED (lxxxix)** — observed as `own_identity_replaced_v1` == `own_identity_since_v1`; HOW it got persisted is not reproduced, and the loaded-keys `initialize` retracts an alarm for exactly `ownIdentitySince` whatever wrote it (`2026-09-23-storage-loss-part-a.md`).
- **A contact's anchor refuses a re-minted peer key until a human compares fingerprints** — `AccountIdentityMismatch` + red pill by design ((xlvi)); only restoring the SAME identity keeps contacts sending after a wipe, and lever (d), which did that, was DROPPED by the owner 2026-09-23 (`2026-09-23-storage-loss-part-a.md`).
- **Every `restore`-authorized upload runs the full §6.2 teardown (new device id, all tokens revoked), even un-enrolled** (`2026-09-23-storage-loss-part-a.md`).
- **Session-rebuild requests live in server memory only** — a backend restart drops them; the replay clears on bundle fetch (`2026-09-23-storage-loss-part-a.md`).
- **A contact's post-wipe message sealed to the dead session is never re-delivered** — Part C was dropped; it renders "Nie można odczytać" (Part A's A2 hide was reverted for exactly this: hidden, it vanished while the sender saw ✓✓) (`2026-09-23-storage-loss-part-a.md`).
- **`ContactState.former` is unreadable below PR1.2** — an older build counts the row UNDETERMINED and its contact backup then refuses to upload; never roll a device holding `former` rows back below PR1.2 (`2026-09-23-metadata-pr12-box-client.md`).
- **libsignal's `Curve.calculateAgreement` THROWS on a low-order point (`x25519` 0.1.1)** — agree on any peer-supplied X25519 key inside a try; `QueueSeal` turns it into null, never a crash (`2026-09-23-metadata-pr12-box-client.md`).
- **`socket_io_client` `dispose()` on a connected socket fires its own `disconnect` synchronously** — detach the socket (null the field) BEFORE disposing, or an intentional close reads as a lost connection and schedules a reconnect (`2026-09-23-metadata-pr12-box-client.md`).
- **Without `forceNew`, every ROOT-namespace `socket_io_client` socket to one origin (a URL with no path) shares ONE socket** — `_lookup` looks for path `''` but the Manager stores the root namespace under `'/'`, so it never sees a match. Two accounts in one process ended up on the same socket and `socketReady` timed out. A path URL (`$base/box`) does find its namespace and gets a new Manager. For the box's forceNew, check the engine ids (`IoBoxSocket.engineId`), not account behaviour (`2026-09-23-metadata-pr13-box-roundtrip.md`).
- **A wire id (`E2eEnvelope.msgId`) is unique only per SENDER — key it `(senderId, wireId)`, never alone** — a peer sees ours in plaintext and can stamp one on its own message; unscoped, a replay that lands FIRST is the only claim and stands in for ours. `_wsid` rides beside `_wid`, taken only from an authenticated sender (`2026-09-24-metadata-pr21-wire-sender.md`).
- **"BOTH record stores" (PR2.1, `task_plan.md` §0) = the content store + the MOBILE legacy secure store** (`_legacyDecryptedContentFallback`), not `_decrypt_raw_v1_`; the content store wins for the same key, as in `getDecryptedContent` (`2026-09-24-metadata-pr21-wire-id.md`).
- **Never derive an E2E-bound id from a server field in `fromJson`** — `wireId: json['sendToken']` let a server plant a wire id on a PEER row, and first-write-wins `_wid` then pinned the lie; take such ids only from the decrypted envelope or from what THIS device minted — even an origin-scoped echo (`messageSent.sendToken`) is a server field (`2026-09-24-metadata-pr21-wire-id.md`, `2026-09-24-metadata-pr21-wire-sender.md`).
- **Web plaintext records are WHOLE-record sealed (`fps1:<kid>:<cid|->:<b64 IV‖ct>`) — `_wid`/`_wsid` are NOT readable in localStorage** — to inspect in a browser: raw AES key `localStorage.FlutterSecureStorage` → decrypt `FlutterSecureStorage.fp_content_key_<flutter.fp_content_active_kid_v1>` (`iv.ct`) → 64-hex content key → AES-GCM over the record (`2026-09-24-metadata-pr21-wire-sender.md`).
- **The PEER path of `getDeviceList` must survive release N (deleted in PR4.1, owner 2026-09-24)** — the old path still fetches peer lists for send/edit resend, the decrypt accept gate, the senderListInfo re-check and failed-send refresh; without it multi-device sends fail after 3 retries and device >= 2 messages are withheld (`2026-09-24-metadata-pr32-first-contact.md`).
- **Every path that hands out a bundle goes through `ChatKeyExchangeService.claimBundle`** — it spends the OTP AND fires `preKeysLow` to that device's room; since PR3.2 `searchUsers` spends one per target device, so it is throttled at the fetch tier (100 / 15 min), never a lookup tier (`2026-09-24-metadata-pr32-first-contact.md`).
- **`ChatReconnectManager` STOPS after `reconnectMaxAttempts` (5, ~31 s) and only a new `connect()` restarts it** — device-found on the Pixel_7 AVD: a 194 s backend restart left the box dead until the app was restarted (the account socket recovered by itself, so nothing called `BoxSession.resume`). `BoxClient` now keeps retrying at `reconnectMaxDelay` until `close()`; any other long-lived socket built on that manager needs the same owner. Emulator airplane mode does NOT cut the link to 10.0.2.2 — simulate outages with `docker restart` (`2026-09-24-metadata-pr31a-box-session.md`).
- **Every rule that destroys plaintext on SERVER evidence must skip local (box) ids** — `getServedMessageIds` reconciliation asked about every stored id, and a box message (local id ≥ 2^48, no server row) reads "not served": every box message would have been destroyed within 6 h. Gate on `isServerMessageId` (`utils/message_ids.dart`); retention and the LRU are clock/count rules and treat local ids like any record (`2026-09-24-metadata-pr31b-box-dispatcher.md`).
- **A box delivery must never wait UNACKED for the app** — the window is 16 unacked per socket across ALL rids and the box re-pushes only after a resubscribe, so 16 withheld (or 16 junk frames on the public request sid) freeze every contact's queue. Journal, then ack, then read; a delivery the journal cannot take is held in RAM and retried on the next drain (`2026-09-24-metadata-pr31b-box-dispatcher.md`).
- **The history FILE (PR2.3) carries `_decrypted_*` verbatim, local ids included** — after a storage loss, box messages received BEFORE the import hold local ids the file may also use; the import keeps one and loses the other. Remap local-range ids on import before release N (owed) (`2026-09-24-metadata-pr31b-box-dispatcher.md`).
- **A box route must never depend on the box being CONNECTED** — gating `addressesFor` on `BoxState.ready` sent every text to a covered peer over the old path whenever the box dropped: a server row naming the pair. Answer from the contact record; a down box fails at `deliver` (`2026-09-24-metadata-pr31c-box-send.md`).
- **A same-user reconnect clears `_sendTokenByTempId`, so an ordinary retry mints a NEW wire id** — harmless for the old path's server dedup, a duplicate for any device a failed box attempt reached; box tempIds (`_boxTempIds`) keep theirs and are pinned to the box (`2026-09-24-metadata-pr31c-box-send.md`).
- **A covered peer whose device list cannot be verified must FAIL the send, not take the old path** — the connect's lookup (decision 21) can fail or time out offline, and an old-path emit socket.io buffers is replayed as a server row on reconnect. Old path only on evidence (unaddressed device in a verified list, a sibling of ours) (`2026-09-24-metadata-pr31c-box-send.md`).
- **A box send must NEVER look up a device list — its PEER's or its OWN — only await the connect's lookup (`BoxDeviceListRefresh`, decision 21)** — a lookup timed by a send hands the server the pair (or, for the own list, the sender) at the moment of the box frame; a send-time "fallback" fetch when the batch failed reintroduces exactly that. The first build still fetched the OWN list on the first send (`getVerifiedDeviceList(ownUserId)`, uncached) — caught only by reading logcat for `DEVICE_LIST_FETCH_EMIT` between typing and `BOX_SEND`. Missing/failed/dropped = failed row, the batch retries on its own backoff (`2026-09-24-metadata-pr31c-device-list-refresh.md`).
- **A `getDeviceList` emitted before `socketReady` is silently dropped** (`handleGetDeviceList`: no socket user → `return`), so it times out after 10 s — and `onE2EReady` on a reconnect fires at TRANSPORT connect, before `socketReady`. The box lookup waits for both (`ConnectionProvider._accountReady`) (`2026-09-24-metadata-pr31c-device-list-refresh.md`).
- **The box connect lookup spends the shared `getDeviceList` throttle (300/15 min) on every reconnect** — one emit per covered contact plus the own list, beside the old path's refetches; with every friend on the box (~50) six reconnects exhaust it and every box send fails. Owed before slice (d) (`2026-09-24-metadata-pr31c-device-list-refresh.md`, task_plan decision 21).
- **Slice (d) must call `refreshBoxDeviceLists()` whenever it stores a peer address** — the connect lookup covers only peers with an address AT connect; a peer covered mid-session fails every box send ("Ponów") until the next connect (`2026-09-24-metadata-pr31c-device-list-refresh.md`).
- **A device on the link gate holds the PRIMARY's token (`socketReady` deviceId 1)** — anything filed server-side under the session's device (request queue, key bundle) waits for E2E-ready ON THAT CONNECT, or it overwrites the primary's row; the rebind adopts the identity on the old token (`2026-09-25-metadata-sibling-queues-a.md`).
- **A request queue is public and the identity store is TOFU** — an own-account frame on it is read only if a PreKey message carries the ACCOUNT identity key (`prekey_identity.dart`), checked BEFORE decrypt: a PreKey decrypt under a stranger's key REPLACES the real sibling session (`2026-09-25-metadata-sibling-queues-a.md`).
- **Never let the server's device list decide who gets a capability** — `ownRequestQueues` says which siblings exist; the self-queue goes only to devices the own VERIFIED list names live, and an own-account frame that cannot be read now is finished on the request queue (anyone can fill it), held only on the self-queue (`2026-09-25-metadata-sibling-queues-a.md`).
- **Never delete a box queue on acks or drain markers while anyone may still send into it** — a `send` to a deleted sid answers ok, so a copy in flight or sent by a second tab from an older row vanishes silently; retire on the TTL (`2026-09-25-metadata-sibling-queues-b.md`).
- **A received box message with a timer needs a destruction stamp AT RECEIPT (clamped send + 1 d)** — the gate `_recordExpiryDeadlineMs` has no never-read fallback on purpose, so an unopened message would be hidden but never destroyed. The first show overwrites the stamp ONCE; clock = send time, never arrival (`2026-09-26-metadata-item3-decision22.md`).
- **A quote's words must come from OUR copy of `(s, w)`, never the peer's `re.x`, and never from a disappearing message** — the peer controls `x`, and a snippet outlives its original unless cleared (item 4's delete-for-everyone clears it, in RAM and in the records) (`2026-09-26-metadata-item3-decision22.md`, `2026-09-26-metadata-item4-item9.md`).
- **A box action whose sender-side settle skips last-writer-wins silently diverges the sender from every peer** — two quick edits settling out of order left the older on the sender's screen and record for good; the settle compares `ts` like the receiver, equal = this edit (`2026-09-26-metadata-item4-item9.md`).
## Auth
- A 409 the client could not SPEAK created a duplicate account — `ApiException` carries the STATUS, `classifyAuthFailure` maps per door, registration signs in and tries the typed credentials on 409 BEFORE refusing (`2026-09-06-session-register-409-diagnosis.md`).
- iOS Safari freezes a tab's socket in the background; the first POST after resume hangs — "server error" reports from iPhone browser tabs are usually this (`2026-09-08-session-c8-register-ceremony.md`).
- **Browsers send `Origin` on SAME-ORIGIN POST, and the dev CORS allowlist (`main.ts:82-99`) accepts only localhost/`192.168.*`/`10.*`** — any https tunnel host hits the else branch and THROWS, which Nest renders as 500 and `classifyAuthFailure` (`auth_provider.dart:130`) maps to `serverError` ("Serwer nie mógł tego teraz obsłużyć"): a backend-looking failure that never reached the backend. curl/Bun probes send no `Origin` and return 201, so the bug is invisible to exactly the check you would reach for — strip it at the proxy (`proxy_set_header Origin "";`), never edit `main.ts` for a rig (`2026-09-22-metadata-logout-relogin-backup-freeze.md`).
- **A timer to a wall-clock boundary can fire ~1 ms BEFORE `Date.now()` reaches it** (timers run on the monotonic clock) — `recoverPassword`'s one `setTimeout` to the next second signed a JWT whose `iat` equalled `passwordChangedAt`'s second, which `JwtStrategy` refuses: a dead token (CI flake on `917845eb`). FIXED: `auth.service.ts` re-checks `Date.now()` after every wait; pinned deterministically by a virtual clock whose timer lands 1 ms short (`auth.service.spec.ts` "LATER second") (`2026-09-23-storage-loss-drop-bc.md`).

## Android / push
- **versionCode floor 20047** (0.2.47 installed on the OWNER'S REAL PHONE `f849cc68` 2026-09-14) and **updates are FORWARD-ONLY**: a lower versionCode is refused (`INSTALL_FAILED_VERSION_DOWNGRADE`) and the only way past it is `uninstall`, which destroys that device's Signal keys + SQLCipher history — never hand a tester an older APK, bump the patch instead. `am force-stop` silently drops FCM; `FLAG_SECURE` blocks screencap; a RELEASE build's dart-defines live in `lib/<abi>/libapp.so`, not `kernel_blob.bin` (that is the debug/JIT path) — **but since `useLegacyPackaging` they are NO LONGER byte-searchable in the raw APK: that entry is deflated, so EXTRACT it first or a present define reads as lost** (`2026-09-02-session-fcm-e2e.md`, `2026-09-14-session-apk-field-test-list-preview.md`, `2026-09-14-session-apk-weight.md`).
- **`--target-platform android-arm64` does NOT make a single-ABI APK** — the Flutter plugin forces `abiFilters` to all three ABIs on every non-split build (`FlutterPlugin.kt:589-598`), so it narrows only `libapp.so`/`libflutter.so` while six third-party natives still ship ×3; measured 57.26 MiB, not the ~40 the runbook once claimed. You also need `ndk { abiFilters }` + `-Pdisable-abi-filtering=true`. The 1000×ABI versionCode offset is exclusive to `--split-per-abi` (`FlutterPlugin.kt:655-677`) and is a ONE-WAY DOOR against the installed floor (`2026-09-14-session-apk-weight.md`).
- **A release build writes the APK to TWO paths** — `flutter-apk/app-release.apk` AND `apk/release/app-release.apk` (+ its `output-metadata.json` and the `.sha1` sidecar). Restoring or discarding a measurement build must cover all of them, or a prod-signed binary with no dart-defines survives in the second path while its metadata asserts a version it does not contain (`2026-09-14-session-apk-weight.md`).
- **A `flutter build apk --debug` probe POISONS the next `--release` build unless you clean** — the generated `frontend/android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java` (untracked, gitignored) registers `integration_test` and `patrol` unconditionally even though `.flutter-plugins-dependencies` marks both `dev_dependency: true`, and their Android artifacts are not on the release compile classpath → `:app:compileReleaseJavaWithJavac` fails with "package pl.leancode.patrol does not exist". `build-android.ps1 -SkipClean` inherits it; the same script WITHOUT `-SkipClean` is green. Never chase this as a dependency bug (`2026-09-14-session-apk-weight.md`).
- **`screencap` writes a 0-byte file on the `Pixel_7` AVD under BOTH the default and `swiftshader_indirect` GPUs, and the software GPU also ANRs the debug app's first frame and later crashes the emulator (`bad color buffer handle`)** — observe through the VM service instead: `flutter run --use-application-binary … --print-dtd`, `WidgetsBinding.instance.ensureSemantics()` via `evaluate`, then `ext.flutter.debugDumpSemanticsTreeInTraversalOrder` gives labels + rects to tap by `adb shell input tap` (×2.625 for device px); `adb shell svc wifi disable && svc data disable` is the offline switch that works without root (`2026-09-20-metadata-pr22-contact-store.md`).
- **`useLegacyPackaging` makes the on-device footprint SMALLER, not bigger, for a universal APK** — only the ONE matching ABI is extracted: phone `base.apk` 105.3 MiB + empty `lib/` vs emulator 48.9 MiB + 37.2 MiB extracted = 86.2 MiB. It would only grow for a single-ABI build. Do not "optimise" it back (`2026-09-14-session-apk-weight.md`).
- **`adb install` rejects an absolute Windows path from the bash tool** (`filename doesn't end .apk or .apex`) — `cd` to the output dir and pass the bare filename; and `adb` must be invoked as `cmd /c "<sdk>\platform-tools\adb.exe …"`, since the bash tool resolves neither the bare name nor the forward-slash path (`2026-09-14-session-apk-weight.md`).
- **A patch install over the top loses NOTHING — measured twice**: on the 0.2.43 emulator run, and then on the OWNER'S PHONE for `20043 → 20044` (`adb install -r`, `firstInstallTime` preserved, device #11 back online 43 s later with NO login, same identity key, `identity_change_audit` still empty). The reinstall-from-scratch that drills do is `pm clear`/uninstall, a fresh-install *reproduction* tool, never the update path (`2026-09-14-session-apk-field-test-list-preview.md`).
- **MIUI refuses synthetic input: `adb shell input` → `SecurityException: Injecting to another application requires INJECT_EVENTS permission`** — the fix is the Mi-account-gated "USB debugging (Security settings)" toggle, so on a Xiaomi the OWNER taps and the agent observes (logcat + a11y tree + prod DB); plain "Install via USB" is enough for `adb install`. Batch tap instructions instead of driving step by step, and remember `FLAG_SECURE` means the owner cannot screenshot either (`2026-09-14-session-apk-field-test-list-preview.md`).
- **Linking JOINS the account identity, it never replaces it** — a newly linked device's key bundle carries the SAME `identityPublicKey` as the primary and `identity_change_audit` stays empty, so no peer sees a key change from a link (device-proven on `bob208` device #11, `2026-09-14-session-apk-field-test-list-preview.md`).
- **A conversation-list preview is NOT a body surface** — `getLastMessagesBatch(convIds, userId)` takes no deviceId and returns the raw entity, so list rows carry no `envelopeStatus`, never get the `kNotLinkedYetMessageLabel` mapping, and `displayAsEncryptedPlaceholder` (needs the legacy ciphertext column) misses every new-model row; route previews through `sentinelPreviewText`, never `sentinelDisplayText` (which declines peer `[encrypted]` by design) (`2026-09-14-session-apk-field-test-list-preview.md`).
- **`am kill` is a NO-OP on a foreground app** — it only reaps background processes, so a push-with-app-killed test must first push the app behind the launcher (`am start -a android.intent.action.MAIN -c android.intent.category.HOME`) and then gate on `pidof <pkg>` returning EMPTY; skipping that measures push to a LIVE app and logs a false PASS. Measure from logcat (`Start proc … FlutterFirebaseMessagingReceiver caller=com.google.android.gms`), correcting for device-vs-server clock skew — 3.54 s on 0.2.44, real phone (`2026-09-14-session-apk-field-test-list-preview.md`).
- **A voice bubble's label is `position/duration`, so a "0 second" report usually means position 0:00** — the a11y tree read `0:00/0:11` for an 11 s clip; ask what the WHOLE label said before hunting a metadata bug (`2026-09-14-session-apk-field-test-list-preview.md`).
- PWA notification regression history: `2026-08-20-session-notif-regression.md`.
- **The permission set Play sees is the MERGED manifest, not ours** — `cd frontend/android && cmd /c gradlew.bat :app:processReleaseManifest` (no keystore needed) → `frontend/build/app/intermediates/merged_manifest/release/outputReleaseAppLinkSettings/AndroidManifest.xml`: 9 permissions where the source declares 3 (`2026-09-13-session-android-release-research.md`).
- **`light_compressor_v2` merges a `dataSync` foreground service we never start** — no `BackgroundConfig` is passed, yet Play would demand an FGS declaration + demo video for it; strip with `tools:node="remove"` or declare dead code (`2026-09-13-session-android-release-research.md`).
- **The link ceremony is SYMMETRIC and the match code is 6 DIGITS** — both sides display a code (`.web.p` / `.android.n`), either may scan or type the other's, and the primary approves; logging in never adds a device (`resolveLoginDeviceId` returns the live primary); an UN-ENROLLED account meets no gate and silently re-mints, so linking must be enabled on the primary FIRST (device-proven 2026-09-13) (`2026-09-13-session-android-release-research.md`).
- **Linking cannot be ENABLED from a browser TAB, but an ENROLLED account can APPROVE from one** — `devices_screen.dart:426` gates the enable button inside the `notEnrolled` branch ("Najpierw zainstaluj Umbra jako aplikację"); the enrolled branch (`:468`) only nudges and still renders `devices-link-a-device` (`:485`). So "install the PWA first" belongs in enable instructions only — and the real gate on approving is `holdsDak` (`:464`), i.e. the PRIMARY device, installed or not (`2026-09-13-session-android-release-research.md`, code re-read 2026-09-13).
- **The device-link gate WAS coverable by a pushed route — fixed 0.2.42 (`23be77d`, #175)** — it is a `Stack` child (`main.dart`), so `DeviceLinkGateRouteGuard` now removes page routes pushed while gated; dialogs and the ceremony's scanner (`kLinkScanRouteName`) are deliberately kept. Do not "simplify" it back to the one-shot edge `popUntil`: a push never rebuilds `AuthGate` (`2026-09-13-session-android-release-research.md`).
- **`getNotificationAppLaunchDetails()` is sticky for the whole PROCESS and `PushService.initialize` runs on EVERY login** — that combination re-delivered one account's notification tap to the NEXT account; latched to once per process in `android_fcm_local_notifications.dart` (`2026-09-13-session-android-release-research.md`).
- **The signing-cert door closes on the first USER INSTALL, not on a published release** — the old test ("`gh release list` empty") is unreachable now that distribution is a cloud link, so it would read "still open" forever. Any device holding an APK signed with this `.jks` can take a Play update ONLY if Play App Signing adopted that same keystore via PEPK; a Google-generated cert refuses those installs and leaves uninstall — i.e. loss of that user's Signal identity and history — as the only path. **Already partly shut:** the owner's phone has run this key since 0.2.43 and he can wipe his own device, but friends cannot be asked to, so the choice (PEPK vs sideload-forever) is owed BEFORE `umbra-0.2.48.apk` is sent (`2026-09-13-session-android-release-research.md`, `2026-09-14-session-apk-weight.md`).
- **The device-link gate only appears when the device holds NO local identity for that account — `pm clear`, NOT logout** — logging out and straight back in with the password lands in the normal shell, because the account's identity keys survive logout on that device. Reproduce the friend's-fresh-phone state with `pm clear` (drill re-run 2026-09-13 on `7818786…`).
- **Enabling linking FORCES a recovery phrase first** — `Włącz łączenie` routes to the recovery-key screen with NO "Później" escape; 12 words + a word challenge must be completed before the device becomes primary. Budget for it in user instructions (drill re-run 2026-09-13).
- **Driving a release APK: `keyevent 111` (ESC) does NOT close Flutter's IME — use `keyevent 4`** — a still-open IME pushes the submit button out of the a11y tree entirely, which reads as "the button does not exist"; check `dumpsys input_method | sed -n 's/.*\(mInputShown=[a-z]*\).*/\1/p'` before concluding anything. **And do not re-tap a button that looks inert: wait and re-dump.** Several actions are async (`_confirmWord` polls a socket round-trip up to 6 s behind a spinner); a re-tap fired during the in-flight action lands on the NEXT screen instead. An earlier draft of this line blamed "the first tap after a dump is swallowed" — that was wrong, both observed cases were slow async actions that had in fact registered (`2026-09-13-session-apk-drill-rerun.md`).
- **Measure push latency from the notification record, not a poller** — `dumpsys notification --noredact` contains an `AppSettings: com.fireplace.app` line that matches any naive package filter and false-positives instantly; take the posted record's `when=` and subtract a device-clock (`date +%s%3N`) reading of the send. Measured 3.16 s and 3.29 s on `7818786…` (drill re-run 2026-09-13).
- **A Play-image AVD refuses `adb root`** (`adbd cannot run as root in production builds`), so `/data/data/com.fireplace.app/` is unreadable and `run-as` is blocked on a release build — the available proof that `install -r` preserved a device is "relaunch lands on the chats screen, not the login screen"; a wiped data dir would force a re-login (`2026-09-18-apk-update-check.md`).
- **Play cannot FORCE an update** — auto-update is a per-device user setting with no server-side switch; the only forcing tool is our own `/version` android block + an in-app minimum-version refusal. A wire compat window (e.g. the legacy plain-emoji reaction branch, `chat.dto.ts:283-292`) closes when that gate ships or the install list is confirmed, never "at launch" (`2026-09-19-metadata-research-and-refactor-plan.md`).
- **A widget that reads a plain static latch registers NO dependency, and a `const` child is not rebuilt with its parent** — the storage-loss screen's mount polled `StorageLoss.lostThisBoot` per build, but the latch is set while the content store opens, strictly AFTER that layer first mounts, so the screen could never have appeared. Expose it as a `ValueNotifier` and use `ValueListenableBuilder` (`2026-09-20-metadata-pr24-pr23-backups.md`).
- **A backup blob's wraps and its ciphertext must move ATOMICALLY or the row becomes unopenable** — a new blob written beside another device's stale wraps is a row nobody's key opens. `PUT /backup/contacts` therefore carries `baseRev` over the whole `(ckId, wraps, blob)` triple and refuses a mismatch; a client that gets 409 with a CHANGED `ckId` must go locked, never force the write (`2026-09-20-metadata-pr24-pr23-backups.md`).
- **A contact-backup blob written before the 4 KiB pad frame landed (`1cfdc7f2`) locks that account FOREVER** — `_unpad` (`contact_backup.dart:355-359`) throws `frame`, and `_adopt` only lets `blob_auth` overwrite, so a pre-padding row can never be replaced by the device that owns it. Correct fail-closed behaviour; when reusing a local test account across that change, delete its `contact_backups` row (`2026-09-21-metadata-phase2a-device-web-drive.md`).
- **A no-op guard keyed on "what THIS session uploaded" cannot fire on the session that restores** — `_uploadedPayload` started null, so a wiped device republished the blob it had just downloaded and moved `rev` + `updatedAt`: a per-storage-loss clock. Seed the fingerprint from the view the server already holds, and make the encoding canonical (contacts sorted by `userId`) or a differing insertion order reads as a change (`2026-09-21-metadata-phase2a-device-web-drive.md`).
- **A backup FILENAME is cleartext metadata even when the payload is sealed** — the export went out through the share sheet as `umbra-<userId>-<date>.umbrabak` into Drive/Gmail/Downloads sync. Names that leave the device must carry no account identifier; the id belongs inside the sealed payload, where the importer checks it (`2026-09-21-metadata-phase2a-device-web-drive.md`).
- **Since PR0.2 the server skips push while the app is visible on ANY chat or the list** — "no notification while I was in another chat" is by design; the server is told only `clientVisible`, the push SW alone learns which chat is open, and `socketReady` re-sends visibility with or without an open chat (`2026-09-22-metadata-lever-b-pr02.md`).
- **Every Web Push must post a notification: Safari REVOKES the subscription after 3 silent pushes** — the box `notifier_challenge` and (decision 39) a `new_message` suppressed for the focused chat go through `postAndClose()` on Apple endpoints; never add a data-only branch to `web-push-sw.js`. Chrome cannot exercise the Apple branch (its endpoint is `fcm.googleapis.com`): prove it by `vm`-loading the SW with a fake `self`, then on an iPhone at G5 (`2026-09-25-metadata-e9-box-push.md`, `2026-09-25-metadata-decisions-37-39.md`).
- **A Flutter tab that navigates away leaves its sockets open; the server sees the disconnect ~40 s later (ping timeout)** — an "app closed" push drive must wait for it; a silently dead box socket kept its queues and nothing pushed (fixed: wake on detach) (`2026-09-25-metadata-e9-box-push.md`).
- **`navigator.serviceWorker` is undefined in an insecure context (a LAN-IP dev origin) and some in-app WebViews** — gate every access on `WebPushBridge.isSupported`; an unguarded getter threw inside `BoxSession.start` and left the box half-wired (`2026-09-25-metadata-e9-box-push.md`).
- **libsignal_protocol_dart 0.8.2 trial-decrypts on a SHALLOW copy of the current session state (`SessionState.fromSessionState`, `session_state.dart:30-32`)** — when a message then decrypts from an ARCHIVED state, the failed try has already moved the current state's root key and chain, so the stored record breaks the fresh session and the next reply fails `Bad Mac`. Reproduced on real libsignal after a sibling re-key; order old-session messages before a rebuild in any test, and never assume "the archive still reads it" is free (decision 37, E37b; `2026-09-25-metadata-decisions-37-39.md`).

## Agent tooling / editing
- **`String.replace` treats `$` in the REPLACEMENT as special** — a doc patch containing `` {1,32}$` `` spliced `CLAUDE.md` into itself twice; use `() => to` (`2026-09-08-session-d27-rename-and-review.md`).
- The `edit` tool needs a tag from a `read`, never from `grep`; in a multi-worktree checkout give `edit` an ABSOLUTE path (`2026-09-08-session-d27-…`, `2026-09-08-session-dependabot-…`).
- **This worktree is shared: other agents/the owner edit it concurrently** — foreign uncommitted diffs appeared mid-session twice (`local/` sweep 09-08; six frontend files 09-10). NEVER `git add -A` here; stage by explicit path and `git show --stat` the commit before pushing (`2026-09-08-session-d27-rename-and-review.md`, `2026-09-10-session-workflow-2.0.md`).
- **NEVER leave a tracked file reverted in this shared worktree for an A/B measurement** — a concurrent session's docs commit (`eec7d98`) staged the temporarily pre-fix `chat_input_bar.dart` and pushed the shipped fix off master; A/B on a separate clone, or restore + re-verify immediately (`2026-09-12-session-composer-focus-drop.md`).
- **COUNT before any fleet claim** — an earlier summary said the fleet was "two devices"; `SELECT count(*) FROM users` on prod said 115. Compat decisions were made on the wrong number; re-verify volatile facts with a command this session (`2026-09-20-metadata-pr22-contact-store.md`).
- **`./backup-db.sh` BEFORE a destructive migration, not after** — Phase 0 deployed at 03:59 and the 04:00 cron ran one minute AFTER; the pre-change rollback point was the previous night's dump (`2026-09-20-metadata-pr22-contact-store.md`).
- **`frontend/CLAUDE.md` sits ~500 bytes under its 28 000-byte cap** — a new §2 line must be a one-sentence pointer into `frontend/docs/`, or the context-budget hook blocks the commit (`2026-09-20-metadata-pr22-contact-store.md`).
- **`verify-context-budget.mjs --worktree` FALSE-BLOCKS on a Windows CRLF checkout** — it measures on-disk bytes, so `frontend/CLAUDE.md` reads 28130 > 28000 while the committed LF copy is 27934 (196 lines = 196 extra bytes). Re-run the checker with no flag (index/HEAD mode, what the hook enforces) before moving any content out (`2026-09-21-metadata-phase2a-device-web-drive.md`).
- **`flutter run -d chrome` cannot be driven by an automated tab** — dwds needs the Chrome instance it launched, and a second client kills it with `AppConnectionException: Failed to establish connection with the application instance`. For a driven web check, `flutter build web` and serve `build/web` statically; a NEW PORT is a fresh origin, which is also the cheapest honest storage-loss simulation (`2026-09-21-metadata-phase2a-device-web-drive.md`). `-d web-server` (debug) is no better: a second origin never finished loading; for TWO accounts serve one release build on `0.0.0.0` and open `localhost` + `127.0.0.1` (two storages, both in the dev CORS list) in a PORTRAIT viewport (`2026-09-24-metadata-pr21-wire-sender.md`).
- **`verify-claude-*-test-counts.mjs` with no args RE-RUNS the whole suite (~12 min) and will ANR a busy emulator** — pass `--log <path>` with the output of the run you already did (`2026-09-21-metadata-phase2a-device-web-drive.md`).
- **`git push origin master` from a worktree whose HEAD is a branch pushes the STALE local `master` ref and prints success** — the feature commit stayed local, no CI ran on it, and the summary already claimed it shipped. AGENTS.md says `git push origin HEAD:master` for a reason; confirm with `git ls-remote --heads origin master`, never with the push tool's own echo (`2026-09-18-apk-update-check.md`).
- **`adb exec-out screencap -p` served STALE frames on the `Pixel_7` AVD** (three reads in a row showed a Chrome window that was no longer foreground, while `dumpsys activity` named the right activity) — `adb shell screencap -p /sdcard/s.png` + `adb pull` is truthful; this is a different failure from the PowerShell redirect corruption above (`2026-09-21-metadata-phase2a-device-web-drive.md`).
- **A `share_plus` export lands in `cache/share_plus/` and dies with `pm clear`** — the AVD share sheet offers no Files target, so pull the file with `adb exec-out run-as <pkg> cat …` BEFORE the wipe, then `adb push` it to `/sdcard/Download` for the import half (`2026-09-21-metadata-phase2a-device-web-drive.md`).
- **A newest-first planning log reads as CURRENT state to anyone who greps it** — on 2026-09-17 a fresh audit of `.planning/multi-device/progress.md` reported D1 (P0) + D2 (P1) as open and the program "not production-ready", because it hit the `🔴 NEW P0/P1` *report* sections (`:312`, `:382`) and never reached the `DONE … FIXED under (xlvii)` verdict at `:152`. Both defects were fixed weeks earlier (`encryption_service.dart:2238`, `acknowledgePeerIdentity:407+`, `peer_reset_recovery_test.dart` 5/0-skip). Code is the record of truth; the file now carries a `⛔ HISTORICAL LOG` banner naming that line pair (`2026-09-17-session-state-audit-cleanup.md`).
- **`git worktree remove` fails with "is not a working tree" when the registration's `gitdir` holds a BACKSLASH path** — `git worktree list` prints the entry quoted and escaped (`"C:\\…\\fireplace-video\\.git"`) and `prune` does not fix it because the directory really exists; rewrite `.git/worktrees/<name>/gitdir` with forward slashes, then remove normally (`2026-09-17-session-state-audit-cleanup.md`).
- **Live browser drive of the local dev build works headless:** `docker-compose up`, `flutter run -d web-server` (spawn via `cmd /c`, `flutter` is a .bat), seed users by `POST /auth/register` + a `conversations` row in the dev DB, then measure Flutter web geometry from `document.activeElement.getBoundingClientRect()` (the real editing host) instead of pixel-reading screenshots; a fresh PORT gives a fresh origin when an old passcode blocks the old one (`2026-09-12-session-composer-focus-drop.md`).
- **Under the bash tool, `cmd /c mklink /J backend\node_modules …` created `backendnode_modules` at the ROOT with a bogus target** — bash ate the backslashes (the real deps were never linked). Pass Windows paths with forward slashes or from Python; remove a junction with `os.rmdir` (link only), never a recursive delete (`2026-09-23-storage-loss-drop-bc.md`).
- Flutter's `flt-semantics-placeholder` never populates from a synthetic click — `observe()` is useless; drive with two isolated Chromes on `--remote-debugging-port`, pixel clicks from screenshots, CDP `Input.insertText` (`2026-09-08-session-c7-final-review.md`).
- A cold Chrome profile needs ~15 s before the first form paints (`2026-09-09-session-c9-phrase-at-the-door.md`).
- **`adb` is not runnable from the bash tool (exit 127, even by absolute path)** — go through `powershell -NoProfile -Command "& '$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe' …"`, or better, define a `Bun.spawn` wrapper inside an eval kernel (it persists across calls; a bash shell function does not) (`2026-09-14-delete-irreversibility-proof.md`).
- **Backticks in a `git commit -m` body are command substitution** — a one-shot `git commit -m "…`location /`…"` from the bash tool ran `location` as a command and committed the message with those words silently DELETED (2026-09-17, commit `f86a43de`). Amending a pushed commit on the shared `master` is worse than the gap, so pass the message via `git commit -F -` with a quoted heredoc when it contains backticks (`2026-09-17-session-security-headers.md`).
- **The owner's MIUI phone answers `am start` but stays keyguarded** — `dumpsys power` reads `mWakefulness=Dozing` with `isKeyguardShowing=true`, and `input keyevent` is blocked (INJECT_EVENTS), so any on-phone leg needs the owner to unlock the device first; plan the run so their part is a handful of taps (`2026-09-14-delete-irreversibility-proof.md`).
- **Once the owner UNLOCKS it, the real phone is fully agent-drivable without touching its app install** — `adb reverse tcp:3000` + a static-served `flutter build web` bundle on another reverse port, `am start` Chrome at it (an intent, not injected input), `adb forward tcp:9333 localabstract:chrome_devtools_remote`, then attach Puppeteer/CDP: mouse and keyboard go over the protocol, so MIUI's INJECT_EVENTS block is irrelevant, and the owner's 0.2.47 install and its Signal keys are never touched. **The CDP target list also contains the owner's REAL prod PWA tab** (`https://fireplace.ignorelist.com` + its push service worker) — pin the dev target by URL and assert `location.origin` inside every script that writes or clears storage, or a token-injection helper wipes their production keys (`2026-09-14-delete-irreversibility-proof.md`).
- **A `flutter run -d web-server` DDC build NEVER mounts on Android Chrome** — all ~1300 `.lib.js` modules load, `$dartLoader` and `_flutter.loader` exist, and then nothing: no `flutter-view`, no canvas, no console error, no pageerror. The same URL renders fine in desktop Chromium, so it reads like a device/network problem and is not. Serve a `flutter build web --release` bundle from a tiny static server instead — it boots in seconds on the phone (`2026-09-14-delete-irreversibility-proof.md`).
- **Machine-level env vars OVERRIDE compose `--env-file`** — this PC exports `ALLOWED_ORIGINS`/`JWT_SECRET`/VAPID, so `staging.ps1` booted the rehearsal with the dev CORS list and a browser on another port got "Brak połączenia"; pass the vars inline on the `docker compose up` command and verify with `docker exec … sh -c 'echo $VAR'` (`2026-09-18-reaction-tokens-driven.md`).
- **PowerShell `>` corrupts binary: `adb exec-out screencap -p > f.png` yields an unreadable PNG** — use `adb shell screencap -p /sdcard/s.png` + `adb pull`; also `input keycombination 113 29` (CTRL+A) does NOT clear a Flutter text field, use `keyevent 123` + repeated `67` (`2026-09-18-reaction-tokens-driven.md`).
- **`git stash push -- <path>` with NO local changes saves NOTHING, so the paired `git stash pop` applies a FOREIGN stash** — reverting an already-COMMITTED fix this way popped a 4-month-old `feature/composer-trailing-send-voice` stash, conflicted `CLAUDE.md`, and made the "mutant check" silently run against the unmodified tree (the mutant "survived" because it was never applied). A/B committed code by content: `git show <sha>:<path>` out, test, write the original back, then `git diff --stat` to prove restoration (`2026-09-18-reaction-tokens-driven.md`).
- Brand sweeps MUST eyeball the RENDERED page — split-span wordmarks defeat grep; re-verify subagent compliance claims, one fabricated its version bump (`2026-08-26-session.md`).
- **Subagents on 7–15 min tasks null-yield ~half the time** (3 of 6 auditors, 2026-09-10) — the work is on disk, the context is intact. Two rules: (1) write the task so the FILE is the deliverable and the yield is a one-line pointer to it (the four researchers did this: 0 losses); (2) if a yield is still null, `hub send … await:true` asking for the report — never rerun (`2026-09-10-session-workflow-2.0.md`).
- `read` of `LATEST.md` truncates lines at 768 chars — measure with `wc`, not by eye; NEVER re-emit a whole entry from the displayed text (the tail, incl. the `➡` pointer, is silently dropped): `sed -n '<n>p'` it, patch only the stale clause, diff the new line against `git show <prev>:…LATEST.md` (`2026-09-10-session-workflow-2.0.md`, `2026-09-12-session-composer-focus-drop.md`).
- **npm override of a dep you also depend on directly needs the `"$pkg"` reference form** — `overrides.multer: "$multer"` + `multer ^2.3.0`; a nested literal is ignored and `npm ls` shows `invalid` (`2026-09-12-session-batch-8-tooling.md`).
- **NEVER override `csv-parse` to v7 in `scripts/smoke`** — artillery 2.0.34 (latest; still depends on `csv-parse ^4`) does `import _csv from 'csv-parse'` in its ESM build, and v7 exports `{ parse }`, so oclif cannot load `run`/`quick`/`run-*` at all (`run is not a artillery command`); `npm ls` looks clean, only running it reveals it. Alert #119 dismissed `not_used` instead: dev-only dep, and the vulnerable `columns` path needs a `payload:` CSV no scenario defines (`2026-09-12-session-alert-119-csv-parse.md`).
- **`/auth/register` throttles 10 per HOUR per IP** (`auth.controller.ts:15` — `limit: 10, ttl: 3600000`; an earlier line here said "15 min", which is the `/auth/login` window) — repeated smoke runs 429; reuse a throwaway account via `FP_SMOKE_USER/PASS` (`2026-09-12-session-batch-8-tooling.md`, corrected 2026-09-13).
- **In the shared worktree, `git commit` ships whatever the OTHER session has STAGED** — explicit-path `git add` does not protect you; run `git diff --cached --stat` and `git reset -q -- <foreign>` before every commit (`2026-09-12-session-batch-8-tooling.md`).
- **`gh run rerun` on an old run cancels the master tip's run via the concurrency group** — rerun the tip's own run id, never an older one (`2026-09-12-session-batch-8-tooling.md`).
- **`./gradlew.bat` from the bash tool resolves against the REPO ROOT, not `cwd`** — use `cmd /c gradlew.bat …` or `sh ./gradlew …`; and `$?` after a pipe is the LAST stage's (`tail`), so check `${PIPESTATUS[0]}` (`2026-09-13-session-android-release-research.md`).
- **`FLAG_SECURE` blocks `screencap` but NOT `uiautomator dump`** — drive release APKs over the a11y tree + `adb input`; `uiautomator dump` hangs while the UI animates, so wrap it in a timeout and retry (`2026-09-13-session-android-release-research.md`).
- **An "empty" `uiautomator` tree on a Flutter release build is almost always YOUR GREP** — Flutter publishes its labels as **`content-desc`, not `text`**, so `grep 'text="…"'` reports one bare `text=""` node on a fully rendered screen. A/B-measured 2026-09-13 on 0.2.43: with `enabled_accessibility_services=null` and `accessibility_enabled=0`, in a FRESH process, the tree is fully populated (7 labelled nodes on the auth screen) — **no TalkBack, no a11y service, no `settings put` is needed** (an earlier draft of this line credited that toggle; it changed nothing). Second gotcha: `adb shell cat /sdcard/d.xml` through a non-tty wrapper can return an EMPTY string — `adb pull` the dump and read the file (0.2.43 pre-flight 2026-09-13).
- **Flutter WEB text input resists synthetic typing** — `page.keyboard.type` lands only the first char once Flutter owns focus; with semantics enabled the `<textarea>` under `flt-semantics` is a PROXY the composer does not read, so `value` + `InputEvent` and CDP `Input.insertText` fill the DOM while Flutter's state stays EMPTY. Judge by SCREENSHOT, never `el.value`; to send deterministically use the emoji picker, which goes through Flutter's own code path (`2026-09-13-session-android-release-research.md`).
- **`adb shell pm clear <pkg>` is the one-call way back to a pristine first-run state** — cheaper than uninstall+reinstall when a form or session gets stuck, and it is exactly the state a friend's fresh install is in (`2026-09-13-session-android-release-research.md`).
- **Two app-mode Chromes at the same default position OCCLUDE each other** — the hidden one reports `visibilityState: hidden` and Flutter stalls (frozen "Odszyfrowywanie…", 20 s skeleton, late taps); set `--window-position` / `Browser.setWindowBounds` and kill stray debug Chromes (`2026-09-12-session-c10-key-change-line-lifetime.md`).
- **A `browser.run` timeout KILLS the tab** — reattach under a NEW name; and `?q=` vision polls flake, so trust pixels (`2026-09-12-session-c10-key-change-line-lifetime.md`).
- **The emulator syncs its clipboard to the Windows host** — so the hacker-mode panel's `Copy` + `powershell -NoProfile -Command "Get-Clipboard -Raw | Out-File …"` is the reliable way to read the WHOLE durable E2E ring off a release APK, no root needed. The a11y tree also carries each row's full text; if a row looks cut off, check your own print slicing before blaming the panel (`2026-09-13-session-post-wipe-fork-settled.md`).
- **Log the web PWA in by INJECTING tokens, never by typing** — `POST /auth/login`, then `localStorage['flutter.jwt_token'] = JSON.stringify(access)` + `flutter.refresh_token` (SharedPreferences web = `flutter.`-prefixed, JSON-encoded) and reload: sidesteps Flutter web's synthetic-typing problem entirely. Clear the rest of localStorage first — a shared browser profile keeps `sig_e2e_<olduid>_*` residue from previous drills (`2026-09-13-session-post-wipe-fork-settled.md`).
- **A second `socket.io-client` joined as the PEER is a wire-level observer** — `auth: { token }`, `onAny`, and it sees everything relayed to `user:<id>`, including the server's PENDING `sessionRebuildNeeded` replay on connect. That answered "was the request emitted?" in 4 seconds after the in-app diag could not (`2026-09-13-session-post-wipe-fork-settled.md`).
- **`keyevent 4` with NO IME open EXITS the app** (back pops the route) — it cost two re-logins; gate it on `dumpsys input_method … mInputShown=true`. Also `uiautomator dump` answers `ERROR: null root node` while the UI animates: retry, do not assume the app died (`2026-09-13-session-post-wipe-fork-settled.md`).
- **An `M` in `git status` can be pure LF→CRLF normalization — run `git diff` before calling a file someone else's uncommitted work** — three generated `l10n/app_localizations*.dart` files read as "another session's edits" for a whole session and were used to justify a blocked plan; `git diff` on them was EMPTY and regenerating reproduced the same bytes plus the new keys (`2026-09-13-session-unreadable-row-copy.md`).
- **`powershell -File <script>` under the bash tool returns a FAKE exit code — `$?` tracks the shell, not the script** — on 2026-09-14 PowerShell refused the file outright ("argument does not exist") and the very same line still echoed `EXIT=0`, which silently defeats the `deploy-web.ps1` exit-21 guard it was written to catch. Bash also eats the backslash in `.\script.ps1` (it becomes `.script.ps1`). Use `powershell -NoProfile -ExecutionPolicy Bypass -Command "& { ./script.ps1; exit $LASTEXITCODE }"`, and judge a deploy by its own success markers (`PUBLISHED_OK`, `SMOKE PASSED`) rather than any exit code (`2026-09-14-e2e-gap-audit-and-deploy.md`).
- **`frontend/l10n.yaml` sets `template-arb-file: app_pl.arb`** — placeholder metadata (`"@key": {"placeholders": …}`) belongs in the POLISH arb; putting it in `app_en.arb` makes gen-l10n report the en type as diverging from an untyped template and the getter is never generated (`2026-09-17-reaction-tokens-end-to-end.md`).
- **A scout's "zero callers" is a hypothesis, not a finding** — two deletion candidates it declared dead had live callers or live platform paths (`clearPendingPreKeyFetch` ← `messaging_provider.send.dart:1617`); run `lsp references` on every candidate yourself before it enters a delete list (`2026-09-19-metadata-research-and-refactor-plan.md`).
- **A line-anchored `PUT >N` at the wrong N splits a multi-line construct silently** — happened twice in one session on planning docs; on Dart a dangling `///` compiles. Re-read the edited region (not the tool echo) after every source edit (`2026-09-19-metadata-research-and-refactor-plan.md`).
- **The god-class refactor was DROPPED by the owner after verification — do NOT start `.planning/refactor-godclasses/task_plan.md`** — it opens with a DROPPED banner and survives only as a verified cluster map; extract `PlaintextRecordStore` on demand if a metadata step needs it (`2026-09-19-refactor-dropped-d10-closed.md`).
- **A `Map<String, Function>` registry for `connection_provider.dart`'s 70 `on()` sites is net 20–55 lines, not "hundreds"** — 58 are one-liners and a forgotten map entry is as silent as a forgotten `on()`; do not re-propose it as a slop win (`2026-09-19-refactor-dropped-d10-closed.md`).
- **`hub start` cannot launch `flutter` directly on Windows (`os error 193`, the `.bat` shim)** — use `application: cmd`, `args: ["/c", "flutter run …"]` (`2026-09-19-refactor-dropped-d10-closed.md`).
- **`npm ci` on npm 11.11 rewrites `backend/package-lock.json` (drops `libc` fields)** — `git checkout -- backend/package-lock.json` before staging; never commit it (`2026-09-20-metadata-phase0-shipped-on-branch.md`).
- **Never regex-sweep whole files, even for log strings** — a tidy rule ate a `to:` property; do explicit per-line replacements and audit every deleted line that is not a log string; apply prettier only at your hunks (eslint `--format json` fix ranges) or the ratchet's formatting count moves (`2026-09-20-metadata-phase0-shipped-on-branch.md`).
- **Multi-worktree: `edit`/`write` with a relative path lands in the CWD checkout, not the one you think** — an nginx comment edit landed in `Fireplace/` while the commit ran in `fireplace-mp/`; use absolute paths (`2026-09-20-metadata-phase0-shipped-on-branch.md`).
- **The Android login form SHIFTS when the IME opens (~370 px here), so a tap computed from the pre-IME dump lands on the wrong widget** — the second field's coordinates hit "Nie pamiętam hasła" and silently drove the recover flow instead of login. Re-dump between every tap, and never `keyevent 4` unless `dumpsys input_method` says `mInputShown=true` (`2026-09-20-metadata-pr24-pr23-backups.md`).
- **Never run `dart format` on a shared file** — `dart format --line-length 80` on a test file reflowed 184 lines of unrelated pre-existing code around a ~50-line addition, burying the change and inviting a shared-worktree conflict; the commit had to be reset, the file restored from HEAD, and the addition re-applied by hand (56 insertions, 0 deletions). Match the file's existing style instead (`2026-09-22-metadata-logout-relogin-backup-freeze.md`).
- **Flutter web's a11y textbox keeps only the FIRST character from Puppeteer `type`/`fill`** — the composer rebuilds on the first keystroke and the editing element is replaced; deliver the whole string with CDP `Input.insertText` after clicking the semantic textbox, and if the send button never appears, leave and re-open the chat to rebuild the composer (`2026-09-22-metadata-run-c-close-2a.md`).
- **A swipe that starts on the open Gboard glide-types letters into the focused field** (` ft` appeared after a 12-word phrase) — close the IME first (`keyevent 4` only under `mInputShown=true`), and clear a field with long-press → `Zaznacz wszystko` → DEL, never `KEYCODE_MOVE_END` (the tap put the caret mid-text) (`2026-09-22-metadata-run-c-close-2a.md`).
- **Split one file across two commits by staging a BUILT blob, never by reverting the shared worktree** — `git hash-object -w --path=<p> <tmp>` then `git update-index --cacheinfo 100644,<blob>,<p>` stages the first half while the working copy keeps both; `--path` applies the CRLF→LF filter (`2026-09-22-metadata-lever-b-pr02.md`).
- **`async: true` does NOT extend the bash tool's 300 s deadline** — a backgrounded CI watcher died at 300 s three times in one session because the call's `timeout` parameter was left out; set `timeout` (0 = no deadline) on the call itself, or run long waits as `hub start` processes (`2026-09-22-metadata-lever-b-pr02.md`).

- **`git hash-object -w --stdin` skips autocrlf** — feed LF bytes, or the staged blob rewrites every line (a 4-line doc edit showed as 6338) (`2026-09-23-storage-loss-part-a.md`).
- **Backend builders recompile the dev app on every write (~2 min reboot, `/health` 000)** — never live-drive or run `test_e2e` while a backend agent is editing (`2026-09-23-storage-loss-part-a.md`).
- **`hub start` with `cmd /c` splits a quoted `-t "a b"`** — jest matched `deletes|it|for|good"` and ran 0 tests; pass a regex with no spaces (`-t a.b.c`) (`2026-09-23-metadata-pr12-box-client.md`).
- **The Python eval kernel rewrites a line that starts with `!` inside a string literal into `__omp_shell(...)`** — a Dart `!socket.connected ||` line landed in `box_client.dart` as a shell call; write code containing `!`-led lines with the `edit` tool, or grep for `__omp_shell` after (`2026-09-23-metadata-pr13-box-roundtrip.md`).
- **Browser-tool calls fail (`Failed to restore browser request interception`) while the page's main thread is blocked** — wait out a known-blocking page before the first `console()`/`evaluate`, and `browser.close({all:true, kill:true})` if it sticks (`2026-09-23-metadata-pr13-box-roundtrip.md`).
- **A dev `docker compose up -d backend` rewrites `backend/package-lock.json` through the bind mount** — the container's `npm install` pruned 96 lines; once `/health` answers, `git checkout -- backend/package-lock.json`, and never commit it (`2026-09-23-metadata-g4-gate.md`).
- **A managed browser tab released by a `tab.run`/`evaluate` timeout can stay ALIVE with its socket** — the stray Bob page read `hidden` when listed, but the release fired no `visibilitychange`, so the server kept its LAST `clientVisible: true` and correctly held every push; it read like a PR0.2 regression. List `browser.pages()` and `page.close()` strays before judging push. Closing a tab normally resets its notification permission, and Chrome then drops the push subscription (410) (`2026-09-23-metadata-g4-gate.md`).
- **Since PR0.2, a device whose app dies WITHOUT `visibilitychange` (crash, OS kill, network loss) loses pushes for EVERY chat until its socket times out** — `isRecipientDeviceVisible` reads the newest socket's last `clientVisible`; before PR0.2 that stale window suppressed only the chat that was open. Prove prod push as "background the app, wait a minute, then send" (`2026-09-23-metadata-g4-gate.md`).
- **CDP `Network.emulateNetworkConditions` belongs to the session that set it** — an offline set on a throwaway `createCDPSession()` survives a later `offline:false` from another session, so the tab never comes back online and a drive reads a stuck row as a bug; use puppeteer `page.setOfflineMode(true/false)` or close the tab (`2026-09-24-metadata-pr31c-box-send.md`).
- **Headless Chrome never hides a page (`bringToFront` leaves it `visible`)** — simulate backgrounding with a `visibilitychange` event plus overridden `document.visibilityState='hidden'`; and `navigator.serviceWorker.ready` never resolves on a page outside the push SW's `/web-push-scope/`, so an evaluate that awaits it hangs until the timeout (`2026-09-23-metadata-g4-gate.md`).
- **The omp browser daemon can fail to start ("Shared browser daemon unavailable")** — drive two headless Chromes directly over CDP with separate `--user-data-dir`s (two isolated devices of one account), portrait 420x880; a plain tab needs `matchMedia('(display-mode: standalone)')` overridden to show "Włącz łączenie" (`2026-09-25-metadata-sibling-queues-a.md`).
- **A scripted rebase resolver must match CRLF markers and refuse to stage a file that still holds `<<<<<<<`/`=======`/`>>>>>>>`** — a `\n`-only pattern left markers that got COMMITTED, and rerere recorded that resolution for replay: abort, delete the `rr-cache` entry, redo with `-c rerere.enabled=false`, then `git diff <old tip> HEAD` must show master's facts only (`2026-09-25-metadata-sibling-queues-a.md`).
- **Never build an `rm -rf` path with `..`** — `rm -rf <Temp>/pbweb/..` deleted this PC's whole `%TEMP%`; delete the exact directory and echo it first (`2026-09-25-metadata-sibling-queues-b.md`).
- **A second headed Chrome window reads `visibilityState: hidden` (Windows occlusion) and Flutter never paints** — launch it with `--disable-features=CalculateNativeWinOcclusion --disable-backgrounding-occluded-windows`; `bringToFront` does not help (`2026-09-25-metadata-e9-box-push.md`).
- **A MINIMIZED headed Chrome window reads `visibilityState: hidden` too, so `PushBoxSource` offers no target and a drive silently registers nothing** — `bringToFront` does not restore it; CDP `Browser.getWindowForTarget` + `Browser.setWindowBounds {windowState:'normal'}` does (`2026-09-25-metadata-e9-batch-per-token.md`).
- **`cat proc://<id>/kill` only READS: the service keeps running** — a throwaway harness stayed up 12 min on a force-dropped DB; stop a service by WRITING to `proc://<id>/kill` (`2026-09-25-metadata-e9-batch-per-token.md`).
- **A drive's release bundle is built WITH the throwaway scaffold** — `frontend/build/web` keeps it after the source is restored; delete that exact directory after the drive, or a later serve drives scaffold code (`2026-09-26-metadata-item3-decision22.md`).
- **After a cherry-pick into `fireplace-mp` the dev `nest --watch` recompiled but did NOT restart the app, so migration 0024 never ran** — check `to_regclass` for the new table; `docker restart fireplace-mp-backend-1` applies it, then `git checkout -- backend/package-lock.json` (`2026-09-26-metadata-item4-item9.md`).
- **The dev backend's CORS allowlist takes `localhost:3000` and `localhost:8080` only, so a second drive origin on 8081 fails** — run two Chrome profiles (separate `--user-data-dir`) on 8080 instead; storage stays isolated (`2026-09-26-metadata-item4-item9.md`).
## Handoff / LATEST budget history
- **2026-08-14, owner decision: per-entry WORD budgets (≤3900 total / ≤700 per entry) were REMOVED** — the shared banner counted as an "entry", so adding a genuinely new binding fact to it got the commit BLOCKED, and the cheapest escape was deleting evidence from a summary written minutes earlier; three sessions in a row burned time on the arithmetic instead of the handoff.
- **2026-09-10, workflow 2.0 re-introduced a per-entry CHAR cap (≤900, hook 1000) — and the 08-14 failure mode is gone by construction:** there is no banner (standing warnings live here, one line each, uncapped), so nothing binding ever has to fit inside an entry; the entry only has to link the dated file. If the cap ever forces deleting evidence again, that is a regression of this exact decision — move the fact here or to the dated file, never trim it away.
- **`verify-context-budget.mjs --worktree` FALSE-BLOCKS on Windows: it measures the CRLF working file, the hook measures the staged (LF) blob** — `frontend/CLAUDE.md` read 28130 > 28000 in `--worktree` while its staged size was 27934 and `git diff` reported it unmodified; the 196-byte gap was one CR per line. Before moving any content out of a file you did not touch, re-run the gate with NO flag (staged mode, exactly what pre-commit runs) (`2026-09-22-metadata-logout-relogin-backup-freeze.md`).
- **Never leave a capped context file near its cap** (owner, 2026-09-24) — squeezing a new line in by trimming to 99% blocks the next writer; when you touch `AGENTS.md` or a `CLAUDE.md`, bring it back to ≤ ~75% of its `verify-context-budget.mjs` limit by relocating detail (rule + pointer stay) (`2026-09-24-metadata-pr31a-box-session.md`).

- **OMP injects only `AGENTS.md`** at depth 0 (it shadows root `CLAUDE.md`); root/tier files are deliberate reads. A `.omp/rules` edit-time rule needs `condition: ".*"` + explicit `scope: "tool:edit(<glob>), tool:write(<glob>)"` — the YAML-list `condition:` shorthand silently registers nothing. Proven by fresh-session probes 2026-09-10 (`docs/research/2026-09-agent-harness-practice.md` §8) (`2026-09-10-session-workflow-2.0.md`). **`.cursor/rules/` was deleted 2026-09-14 (Cursor is no longer used)** — rules now live in `.omp/rules` + `.claude/rules` only; `.cursor/session-summaries/` is NOT Cursor's and stays (`2026-09-14-session-skills-mcp-audit.md`).
- **`.claude/skills/` IS this repo's skill set; `.omp/config.yml` (`skills.enableAgentsUser: false`) mutes the machine-wide `~/.agents` tree here** — and `.gitignore` ignores `.omp/*`, so the gate needs an explicit `!.omp/config.yml` or it stays machine-local and every other worktree silently differs. Project settings load from the CWD's `.omp/` only, never an ancestor: start `omp` at the repo root or the gate is off. The gate does NOT cover Claude Code, where **personal beats project** — a `~/.claude/skills/<name>` junction shadows the committed copy, so never vendor a skill while a same-named home entry survives. Two filters caused the original mess: `enabledProviders` defaults to `[]` so foreign USER roots contribute NOTHING to OMP while project roots load, and `disable-model-invocation: true` hides a skill from the list while `skill://`/`/skill:` still work (`2026-09-14-session-skills-mcp-audit.md`).
- **Probe skills/MCP with a fresh headless session, ask for the REAL tool name, and retry once** — `omp -p --no-session --tools read` lists discovered skills (discovery is startup-only, so a running session never shows a new one) while `--no-tools` suppresses the list entirely; asking a smol model whether "`analyze_files`" exists gets NO because the route is `mcp__dart_analyze_files`; and `mcp:dart` can lose a cold start with "Transport closed" and mount fine on the next run. Three bisects were spent on that phantom — one NO is not evidence of a config error (`2026-09-14-session-skills-mcp-audit.md`).
- **Deleting a Windows junction with `rm -rf` can take the TARGET with it** — remove the link only: `[System.IO.Directory]::Delete('<path>', $false)` via powershell, then assert the target still exists. `~/.claude/skills` held 10 junctions into `~/.agents/skills`; 8 were removed this way with all 38 targets intact (`2026-09-14-session-skills-mcp-audit.md`).
- **A third-party skill body can be silently mangled — check it against upstream before trusting it** — the installed `planning-with-files` was a `.Codex` find-replace of `OthmanAdi/planning-with-files` whose 456-line body asserted "Codex shipped three new turn-loop primitives" and cited `anthropics/Codex` issues; deleted 2026-09-14. Its `hooks:` frontmatter was inert anyway: OMP loads hooks only from `.omp/hooks/pre|post/*.ts`, so all that shipped was the false prose (`2026-09-14-session-skills-mcp-audit.md`).
- **Never wire an MCP server from `modelcontextprotocol/servers-archived`** (postgres, github, puppeteer, redis, sentry, slack, sqlite, gitlab, brave-search…) — that repo states no security updates will EVER be issued, yet the active servers repo's own "Getting Started" block still shows the archived `server-github`/`server-postgres` entries and most tutorials copy it (`2026-09-14-session-skills-mcp-audit.md`).
- **Delivering a new skill into a busy worktree: copy it UNTRACKED, and delete the copy before that worktree merges the commit that tracks it** — even a byte-identical untracked file ABORTS `git merge` ("would be overwritten"). (`fireplace-mp` has TRACKED it since `1a5c9430`, so this no longer applies there) (`2026-09-23-session-start-skill.md`).
- **Never put a path that is tracked anywhere into `.git/info/exclude`** — it is shared by EVERY worktree: `git add <path>` then exits 1 (stages anyway, but breaks `add && commit`), and the built-in `grep`/`glob` hide the file. Count skills with `git ls-files .claude/skills`, not a gitignore-aware glob (`2026-09-23-session-start-skill.md`).
- **Subagents (scout/reviewer) start in the MAIN checkout, not the worktree you are in** — a scout mapped `fireplace` instead of `fireplace-mp` and reported the box client "does not exist yet"; name the absolute worktree path in every task and distrust answers phrased as inference (`2026-09-24-metadata-pr31a-box-session.md`).
- **`hub start` with application `bash` resolves to WSL's bash on this PC and dies (`execvpe(/bin/bash) failed`)** — run long suites as `cmd /c "flutter test … > ..\log.txt 2>&1"` and judge the log, not `%ERRORLEVEL%` (a one-line `cmd` expands it before the command runs) (`2026-09-24-metadata-pr31a-box-session.md`).
- **`flutter build web -o <dir outside the project>` fails in `impellerc` ("Could not write file … shaders/*.frag"), not in Dart** — the error only says "Failed to compile application for the Web"; build into the default `frontend/build/web` (gitignored) and serve that (`2026-09-24-metadata-pr31b-box-dispatcher.md`).
- **Test a handoff with a blind quiz, not by rereading it** — fresh agents following session-start answered ~60% of this session's questions until the start skill read the dated summary and the summary stopped leaving facts only in the gitignored baton (`2026-09-25-metadata-sibling-queues-b.md`).
- **Never label helper splits with letters: the plan's slices are (a)–(g)** — "slice A/B" for two helpers of item 3 made the owner think the plan had moved to slice (d). Say "item N / <part>" (`2026-09-26-metadata-item3-decision22.md`).

## Owner-owed decisions (open)
- **Metadata-privacy decisions live in ONE tracked log on the branch — read it before asking or answering ANY metadata-privacy question:** `git show origin/feat/metadata-privacy:docs/plans/metadata-privacy-decisions.md`. The 16 MiB cap was asked twice with opposite answers because decision 9 lived only in a gitignored plan in another worktree. Owner answers only OWNER-class questions, batched per phase (S8, 2026-09-25).
- README screenshot recapture, the domain decision (`2026-08-26-session.md`). **The GitHub repo rename is DONE — `Lentach/Fireplace` → `Lentach/Umbra`, 2026-09-14, owner's call** (`2026-09-14-session-apk-weight.md`).
- Thief-with-password matrix answers (`2026-09-03-session-lxxii-reset-door.md`).
- iPhone PWA camera recording for the WebKit probe fix; iOS eyeballing of reset-ceremony statuses, revoke/mismatch notices, fingerprint sheet, phrase reveal (`2026-09-08-session-c8-register-ceremony.md`).
- **A hard min-version gate (`/version.android.minVersionCode` + a full-screen block) was BUILT and REVERTED 2026-09-19 on the owner's word** — while he is the only tester the dismissible banner is enough, and Play testing tracks will auto-update once a developer account exists. Re-open only if sideloaded friends ever need forcing (`2026-09-19-refactor-dropped-d10-closed.md`).
- **Guarded recovery-phrase REPLACE — proposed, brainstormed 2026-09-12, owner picked nothing.** Options A (plain) / B (no button; "Zgubiłem frazę" → warning → password) / C (B + old phrase) / D (never); D locks out anyone who lost the paper. Needs `recoveryPhraseCreatedAt` on the wire → BOTH tiers, spec (lxxxv) first (`2026-09-12-session-c10-key-change-line-lifetime.md`).
- Android release is re-planned but UNANSWERED: Play vs sideload-forever, Play Console account status, the un-enrolled re-mint hazard (mechanism or wording), report-user UX, who writes privacy policy + ToS, R8 for libsignal/drift/Firebase (`2026-09-13-session-android-release-research.md`).
- `dependabot.yml` group fix UNPROVEN; Kaspersky not running + Defender stood down — the dev box is unprotected; 5× Kernel-Power 41 (`2026-09-08-session-dependabot-sweep-and-pc-health.md`).
- Install the Mend Renovate app, then delete `.github/dependabot.yml` in the first green Renovate PR; Patrol web leg still not green; the artillery socket smoke is **4/5 locally** (one reproducible `errors.response timeout`, `vusers.failed == 0` red on `e5cd287`) despite the batch-8 "5/5" claim (`2026-09-12-session-batch-8-tooling.md`, `2026-09-12-session-alert-119-csv-parse.md`).
- **Post-wipe/restore rough edges (2026-09-13; (a) RE-MEASURED and FIXED 2026-09-22):** (a) a peer's LIVE client kept its in-memory verified list for the restored account (v1, the revoked device) for as long as it stayed open — NOT "~90 s": measured >5 min of `SEND_FAIL {Recipient has no key bundle (userId=114 deviceId=1)}` every 4 s shown as `Ponów`, every row from the new device withheld (delivered + READ server-side, never rendered), and a reload healed it but DROPPED the unsent row. Root cause is structural and must not come back: `deviceListChanged` reaches the own room only, `staleLists` never fires because the send dies at the pre-key fetch, and the in-band `senderListInfo` claim rides INSIDE the E2E plaintext so a withheld row can never carry it — a restore is the one event that leaves the peer NO surviving device to send a fresh claim. Fixed client-side (F1 invalidate+refetch on `no key bundle`, F2 one rate-limited verified refetch before withholding an unknown origin device, F3 `sessionRebuildNeeded` invalidates the list); any new accept/send gate keeps that one-refetch rule (`.planning/metadata-privacy/findings.md` §2026-09-22). (b) the gate's reset copy says "nowe klucze po 6 h" while `frontend/docs/e2e-invariants.md` says the phrase shortens it to 1 h — one is stale; (c) `Włącz łączenie` forces regenerating an existing recovery phrase, with no "I already have it" path (`2026-09-13-session-post-wipe-fork-settled.md`).
- **Emoji reactions / D10: ANSWERED and SHIPPED 2026-09-18 in `0.2.49`** — the server stopped storing the emoji character. A reaction is now `base64url(HMAC-SHA256(K_react, "umbra.reaction.v1" || emoji)[0..15])`; `K_react` reaches each participant device through the `reaction_keys` mailbox (migration `0018`, server-assigned epoch on `conversations`). Migration `0018` DROPPED every historical plaintext reaction on prod — owner-approved, irreversible, backup `chatdb-20260918T024051Z.dump.gpg` predates it. **Still open, smaller:** the legacy plain-emoji wire branch stays for one compat window and dropping it is what finally closes D10 (`backend/src/chat/dto/chat.dto.ts:281-291`); `envelopeRefusal` checks shape but never fan-out COVERAGE, so a crafted client can advance the epoch with envelopes reaching nobody and blind the peer's chips (`chat-reaction-key.service.ts:238-257`); a device linked after distribution renders placeholders until the same-epoch top-up is built (`2026-09-18-reaction-tokens-driven.md`).
- **⛔ Play App Signing: PEPK is INTENDED, deferred 2026-09-18 on the $25 Play account — so keep `fireplace-release.jks` and NEVER mint a second keystore.** An installed APK only accepts a Play update if Play App Signing adopted THIS keystore through PEPK (`pepk.jar --keystore=fireplace-release.jks --alias=fireplace --encryptionkey=<Google-provided>`); let Google mint its own key instead and every existing install is refused on certificate mismatch, with uninstall — which destroys that user's Signal identity and history — as the only path. Nothing needs paying or running today; the deferral costs nothing AS LONG AS the keystore survives, and the owner's own phone (`f849cc68`, history since 2026-09-13) is already one of the installs at stake. PEPK adoption is itself one-way: Play will not release the key afterwards (`docs/runbooks/android-release.md:465-476`, `2026-09-18-apk-update-check.md`).
- **G0 metadata Phase 0 — DONE 2026-09-20** (master `f27c5712` deployed, migrations 0019/0020 applied, 115 accounts). **Line budgets on every PR were DELETED by the owner the same day** ("build proper code without bandwidth") — never re-introduce a "over ⇒ STOP" line gate (`2026-09-20-metadata-pr22-contact-store.md`).
