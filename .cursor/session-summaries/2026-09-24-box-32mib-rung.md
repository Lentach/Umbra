# The box media ladder gains a 32 MiB rung, so today's 20 MiB files keep sending once media moves into the box

**Date:** 2026-09-24 · **Version:** unchanged · **Tiers deployed:** none (app); prod nginx `/box/` block applied on the owner's later go-ahead

## What was done
- Session start: no baton here; the local LATEST was 46 commits behind master. `feat/passcode-lock` fast-forwarded `25542d98 → ec69e7a5` (owner's go-ahead) and was pushed; `master` = branch.
- Prod push check, read-only: backend `0.2.51 / 14c1f22d` logged `Firebase Admin initialized` + `Web Push VAPID initialized` at boot. Since then: 0 ERROR lines and 0 push-failure warnings. All 35 WARN lines are `[send] REFUSED` bounces (21 `legacy send to an enrolled party v10`, 2 `v10,v1`, 9 `stale device list v10` / 24 h). That is the designed refuse-and-resend path in `chat-message.service.ts` (~:418). 138 messages were stored in 24 h, the last after the last refusal. The owner's phone background-push check was already recorded as PASSED (mp `task_plan.md` decision 7).
- Cap decision: the owner first said "accept the gap", then "do 32 MiB". That matches mp decision (9), "raise now"; the plan needed no edit.
- `backend/src/box/box.constants.ts` `BOX_MEDIA_LADDER` gets `{32 MiB, '32m'}`. The budget comment now says 32 top-rung files per day (1 GiB + 32 MiB < 2^31, so the integer column holds). Controller and entity docs now say up to 32 MiB / `32m`.
- `frontend/lib/services/box/box_wire.dart` `kBoxMediaLadder` gets 32 MiB.
- `infra/nginx/fireplace.conf`: new `location /box/` with `client_max_body_size 32m` (this block only; the vhost stays 21m), `proxy_request_buffering off` and `X-Real-IP`, and no Authorization. **Applied on the VM** after the owner's go-ahead (below).
- `docs/contracts/wire.md` box media + OFF-on-prod lines; the owner-owed trap was replaced by a standing one in the media group.

## Key files
- Edited: `backend/src/box/{box.constants.ts,box-media.controller.ts,entities/box-media.entity.ts,box.int-spec.ts}`, `frontend/lib/services/box/box_wire.dart`, `infra/nginx/fireplace.conf`, `docs/contracts/wire.md`, `docs/agents/traps.md`
- Read only: `../fireplace-mp/.planning/metadata-privacy/task_plan.md:536-560` (owner decisions 7, 9), `../fireplace-mp/.cursor/session-summaries/NEXT.md` (mp is on sibling queues; media over the box comes later, so there is no overlap with this change)

## Verification
- Red first: the new int test "stores the 32 MiB top rung" got `Expected: 201, Received: 413`. Green after the rung: `npm run test:int` (dev DB `:5433`, `fireplace-mp-db-1`) passed 24/24. The "over the top rung" case now uses 32 MiB + 1, so the refusal still sits at the real top.
- `tsc --noEmit` clean; ESLint on the 4 box files: no output; `jest src/box` 16/16; `verify-box-imports` OK; `dart analyze` of box_wire/box_client: no issues; `flutter test test/services/box` 32/32.
- nginx smoke: `nginx:alpine` loaded the EXACT `/box/` and `/media/` blocks extracted from the tracked conf, in front of a counting node upstream. `nginx -t` passed. Results:
  - `/box/media` 32 MiB → 201, upstream counted 33554432 bytes and saw X-Real-IP.
  - `/box/media` 32 MiB + 1 → nginx 413.
  - `/media/upload` 21 MiB + 1 → 413; 20 MiB → 201.
  - A headers-only POST (CL 32 MiB) to `/box/media` got the UPSTREAM's `413 Payload Too Large` with no body sent, so buffering is off and the header-first refusal works through nginx.
- CI on `191fc86a`: 6/6 success (Analyze, Backend tests, Flutter analyze+tests, E2E wire harness, Web Lock probe, E2E isolated probes).
- PROD nginx apply (owner: "apply it, with a checkpoint"): scp'd the committed conf, then CRLF-stripped it. `diff` live → candidate showed +23/-0, the `/box/` block only. The security snippet was unchanged. Checkpoint `~/nginx-fireplace.bak.20260925T004016Z` (sha `4b141757…`, same as the live file) → `install` → `nginx -t` ok → reload → active.
  - Before → after, same probe script: `GET /box/media/x` 200 index.html → 404 Nest JSON; `POST /box/media` 4 KiB 405 → 404 Nest JSON.
  - An exact 32 MiB POST passed nginx, and Nest answered 404 after only 64 KiB uploaded.
  - 33 MiB → nginx 413; `/media/upload` 22 MiB still 413 (vhost 21m intact).
  - `/`, `/health`, `/version`, `/version.json`, `/main.dart.js`, `/welcome/`, `/apk/`, `/socket.io` poll, `/users/me` 401: identical statuses/sizes before and after. `/` security headers present; `/health` has helmet only.
  - `40/box,` → `44/box {"Invalid namespace"}`: the box is still off.
  - `post-deploy-smoke.mjs --commit d37e2dcc`: 8/8 PASS. nginx error log empty; backend 0 WARN/ERROR in 10 min, healthy.
  - Rollback: `sudo cp ~/nginx-fireplace.bak.20260925T004016Z /etc/nginx/sites-available/fireplace && sudo nginx -t && sudo systemctl reload nginx`.
- NOT verified: a padded upload from the app (no media-over-box slice yet).

## Notes for next session
- nginx `/box/` is LIVE. Turning the box on (PR3.1) still needs the global byte ceiling and the per-socket rid cap first.
- The mp branch must rebase over this commit. It touches `wire.md` lines 77-78 and `box_wire.dart`; mp's own diff edits `wire.md` elsewhere.
- The client check "too large before encrypting" stays owed for the media slice: plaintext ≤ 20 MiB (`MediaCryptoService.maxBytes`) must pad into ≤ 32 MiB.
- Traps → `docs/agents/traps.md` (1 line, media group).
