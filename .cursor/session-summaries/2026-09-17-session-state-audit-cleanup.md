# The multi-device + codelock program was already finished; what was left was litter and two stale tracking docs

**Date:** 2026-09-17 · **Version:** unchanged (0.2.48) · **Tiers deployed:** none

## What was done
- Falsified a fresh audit's claim that D1 (P0) / D2 (P1) are open: both are FIXED under amendment (xlvii) — `encryption_service.dart:2238` reads `getAccountIdentity(peerId)`, `acknowledgePeerIdentity` (`:407+`) clears the warning only when the anchor advanced, and `peer_reset_recovery_test.dart` pins it (5 tests, 0 skips). `progress.md:152` said so all along.
- Killed the branch/worktree litter: **25 → 3 local branches**, **20 → 3 remote heads**, **7 → 3 worktrees**, every deletion content-verified against master. **Recovery SHAs, tracked here on purpose** (`.planning/` is gitignored, so a pointer alone would leave no way back): `c8f662eb` video-nits-0.2.3 · `d5536480` frozen-page-reload · `b306c995` pwa-notification-regression · `487be703` attachment-popover-anchor · `1745a50e` cosmic-theme · `f86a43de` unreadable-reason · `f482debb` proof/e2e-encryption. The 11 other remote deletions were all `ahead=0`.
- Deleted `docs/ISSUE-BOARD.md` (2 months stale, named 6 dead branches, marked shipped work IN-PROGRESS, and its own header said it should never have been in a public repo; the tracker is GitHub Issues). Salvaged everything it was the ONLY record of: the emoji-reaction row → `traps.md` § Owner-owed (**the E2E-vs-metadata decision is the last thing holding D10 open**); its three unverified UI bugs → issues **#176 / #177 / #178**, filed afterwards, `needs-triage`, each naming what changed since. Its two deferred rows are closed by shipped features (`POST /auth/recover`; recovery phrase + linking).
- Added a `⛔ HISTORICAL LOG` banner to `.planning/multi-device/progress.md` naming the exact line pair (`:152` verdict vs `:312`/`:382` reports) that misled the audit, so the next reader cannot repeat it.

## Key files
- Edited: `docs/agents/traps.md` (+3 lines), `.github/workflows/ci.yml` (new isolated-probes step + budget), `frontend/test_e2e/encryption_proof_test.dart` (gate + hooks), `CLAUDE.md` §3 counts, `docs/research/2026-09-mcp-servers.md:198`, `LATEST.md`, `.planning/multi-device/progress.md` (untracked banner).
- Deleted: `docs/ISSUE-BOARD.md`. New: this file + `.planning/2026-09-17-cleanup/findings.md` (untracked).
- Read only (load-bearing): both `docs/audit/` files (untracked — never copy their specifics into a tracked file), `rule://production-vm-deploy`, `CLAUDE.md` §2–§9, `skill://dart-test-fundamentals`.

## Verification
- **Tree health at `8dca7f79`, this session:** `npm test` 62/62 suites **1127/1127** · `tsc --noEmit` exit 0 · lint ratchet PASS at the 898 floor (warnings 341 exact; prettier 151 vs 153, the documented CRLF delta) · `flutter analyze` 0 errors / 0 warnings · `flutter test` **2144 passed, 14 skipped** · both count verifiers OK. **Zero drift** from `CLAUDE.md` §3.
- **CI:** 6/6 `success` on `1f05e3e6` (the pre-session code commit), then **6/6 `success` on `b2055eed`**, my CI fix — including the two jobs that were red on `f86a43de`.
- **Prod, re-measured today:** `/version` and `/version.json` both `0.2.48 / eefc44b0`; `/health` `{"status":"ok","db":"ok"}`.
- **Per-branch deletion reasoning: `.planning/2026-09-17-cleanup/findings.md`** — the same commits under different hashes, or the successor file plus its test, on master in every case.
- **Stashes inspected, NOT dropped** — SHAs `280e4574` / `8fe3a172` / `8004f534` / `d9f0fe5c` (2026-05-24/25, deleted branch `feature/composer-trailing-send-voice`). TWO verdicts: trailing-send is superseded by the hex redesign, but **hold-to-record LOCK is genuinely unshipped** (no lock concept in `recording_controller.dart`, no lock test anywhere). Unmergeable as diffs regardless — a fresh implementation, not a stash pop.
- **Dependabot, authoritative:** `git ls-remote --heads origin 'dependabot/*'` is EMPTY — the 12 were already gone on GitHub; only stale local tracking copies survived. The group fix is still UNPROVEN, Renovate still owed.
- **The security headers WENT LIVE mid-session, so my earlier "GET / returns only HTTP/1.1 200" is FALSE.** Re-measured at the end: HSTS, `nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: no-referrer`, COOP, a full `Permissions-Policy`, and `Content-Security-Policy-Report-Only` (report-only BY DESIGN). Shipped by a concurrent session and applied on the VM; exact values in the findings file.
- **NOT verified by me:** the session-lock probe (headless-Chrome; green in CI), iOS, and the `0.2.48` APK on the owner's phone (still `0.2.47`). I ran no deploy and mutated no prod state.
- **The 429 fix, proven on the owner's live dev stack.** Decisive check: ran the proof with **Docker not running at all** → 5/5 skipped, exit 0, `requireBackendUp` never reached, zero registrations. Shared run then gave **46 passed / 14 skipped, no 429**. The proof with its define gives 4 of 5 locally; the census fails on ONE pre-existing drill row (id 977) absent from CI's empty DB — assertion NOT weakened, row NOT deleted. Transcript + repeat-sequencing: findings file.
- The ~890-line diff on `encryption_proof_test.dart` is `dart format` reindentation from the group wrap, not logic. `backend/package-lock.json` had the documented libc-only npm churn and was RESTORED, never committed.

## Notes for next session
- **A concurrent session shipped three of the four follow-ups WHILE this one ran** — unreadable-reason, the encryption proof, and the prod vhost + headers, ending with the vhost applied on the VM (`5f1b14a9`, `4dac5585`, `f86a43de` → `ace8bf5d`). My rebase of `feat/unreadable-reason` came out EMPTY, so both branches and the `fireplace-e2eproof` worktree were swept too. Their `fireplace-csp` worktree was LEFT ALONE while it existed. **Still open: the `_reenrollAfterReset` latch and D10 reactions.**
- **Their `f86a43de` turned master RED and I fixed it (`b2055eed`, CI 6/6):** the proof suite's 2 extra `/auth/register` calls tipped the shared `test_e2e` run over the 10/hour/IP bucket `ci.yml:292-297` already called spent, so a 429 hit whichever suite ran last. Per that file's rule ("never raise the production cap to fit a test") the proof moved to `e2e-isolated-probes` — 6 of 10, gated on `--dart-define=ENCRYPTION_PROOF`, hooks INSIDE the group, siblings' passing-test guard.
- Sealed sender is closed as a direction. If metadata work is wanted, it is RP1 (ephemeral-by-default) + RP2 (padding buckets) from the 2026-07-07 roadmap — and QW0/QW1/QW2 are already largely done (docker log caps `10m×3`, the L1 send log + L2 roster dump are gone, FCM `senderName` and the web-push topic are stripped; only `conversationId` remains).
- Owner-owed still open: signing-cert choice (PEPK vs sideload-forever) before the `0.2.48` APK is handed out; the `0.2.48` APK was never smoked (phone is on `0.2.47`); recovery-phrase replace A/B/C/D; iOS everywhere.
- Traps (appended to `docs/agents/traps.md`): the emoji-reaction decision blocking D10; a historical planning log's newest-first sections read as current state; `git worktree remove` fails on a backslash `gitdir` path; the shared `test_e2e` register budget is 10/hour and adding a suite to it is what turns an untouched file red; `addTearDown` inside `setUpAll` fires when `setUpAll` completes.
