# The multi-device + codelock program was already finished; what was left was litter and two stale tracking docs

**Date:** 2026-09-17 · **Version:** unchanged (0.2.48) · **Tiers deployed:** none

## What was done
- Reconstructed repo state WITHOUT resuming the prior heavy sessions (owner: "blackholes of token drain"). Verdict: `feat/passcode-lock` is byte-identical to `origin/master` (`8dca7f79`), so multi-device + passcode lock ARE master; nothing was left half-built.
- Falsified a fresh audit's claim that D1 (P0) / D2 (P1) are open: both are FIXED under amendment (xlvii) — `encryption_service.dart:2238` reads `getAccountIdentity(peerId)`, `acknowledgePeerIdentity` (`:407+`) clears the warning only when the anchor advanced, and `peer_reset_recovery_test.dart` pins it (5 tests, 0 skips). `progress.md:152` said so all along.
- Killed the branch/worktree litter: **25 → 3 local branches**, **20 → 3 remote heads**, **7 → 3 worktrees**, every deletion content-verified against master. **Recovery SHAs, tracked here on purpose** (`.planning/` is gitignored, so a pointer alone would leave no way back): `c8f662eb` video-nits-0.2.3 · `d5536480` frozen-page-reload · `b306c995` pwa-notification-regression · `487be703` attachment-popover-anchor · `1745a50e` cosmic-theme · `f86a43de` unreadable-reason · `f482debb` proof/e2e-encryption. The 11 other remote deletions were all `ahead=0`.
- Deleted `docs/ISSUE-BOARD.md` (2 months stale, named 6 dead branches, marked shipped work IN-PROGRESS, and its own header said it should never have been in a public repo; the tracker is GitHub Issues). Salvaged everything it was the ONLY record of: the emoji-reaction row → `traps.md` § Owner-owed (**the E2E-vs-metadata decision is the last thing holding D10 open**); its three unverified UI bugs → issues **#176 / #177 / #178**, filed afterwards, `needs-triage`, each naming what changed since. Its two deferred rows are closed by shipped features (`POST /auth/recover`; recovery phrase + linking).
- Added a `⛔ HISTORICAL LOG` banner to `.planning/multi-device/progress.md` naming the exact line pair (`:152` verdict vs `:312`/`:382` reports) that misled the audit, so the next reader cannot repeat it.
- Declined the owner's planned next feature (**sealed sender**) with the repo's own evidence: rejected by `docs/audit/2026-07-07-metadata-privacy-audit.md:340-346` and re-rejected by `2026-09-14-e2e-gap-audit.md:178-183`. Owner accepted, and redirected this session to the open fixes + cleanup instead.

## Key files
- Edited: `docs/agents/traps.md` (+1 Owner-owed line), `docs/research/2026-09-mcp-servers.md:198` (dangling board reference), `.cursor/session-summaries/LATEST.md`, `.planning/multi-device/progress.md` (untracked, banner).
- Deleted: `docs/ISSUE-BOARD.md`.
- New: this file.
- Read only (load-bearing): both files in `docs/audit/` (untracked — never copy their specifics into a tracked file), `docs/agents/traps.md`, `rule://production-vm-deploy`, `CLAUDE.md` §2–§9.

## Verification
- **Tree health at `8dca7f79`, run this session:** `npm test` 62/62 suites **1127/1127** · `tsc --noEmit` exit 0 · lint ratchet PASS at the 898 floor (warnings 341 exact; prettier 151 vs 153 = the documented CRLF delta) · `flutter analyze` 0 errors / 0 warnings · `flutter test` **2144 passed, 14 skipped** · both count verifiers OK. **Zero drift** from `CLAUDE.md` §3.
- **CI:** 6/6 `success` on `1f05e3e6` (the pre-session code commit), then **6/6 `success` on `b2055eed`**, my CI fix — including the two jobs that were red on `f86a43de`.
- **Prod, re-measured today:** `/version` and `/version.json` both `0.2.48 / eefc44b0`; `/health` `{"status":"ok","db":"ok"}`.
- **Per-branch deletion reasoning: `.planning/2026-09-17-cleanup/findings.md`** — the same commits under different hashes, or the successor file plus its test, present on master in every case.
- **Stashes inspected, NOT dropped**, recovery SHAs `280e4574` / `8fe3a172` / `8004f534` / `d9f0fe5c` (2026-05-24/25, deleted branch `feature/composer-trailing-send-voice`). TWO verdicts: trailing-send is superseded by the hex redesign, but **hold-to-record LOCK is genuinely unshipped** (no lock concept in master's `recording_controller.dart`, no lock test anywhere). Unmergeable as diffs either way — see the findings file.
- **Dependabot, authoritative:** `git ls-remote --heads origin 'dependabot/*'` is EMPTY — the 12 were already deleted on GitHub; only stale local tracking copies survived, pruned by the `fireplace-0a` pull. The group fix is still UNPROVEN and the Renovate migration still owed.
- **NOT verified:** the session-lock probe (headless-Chrome, not run locally; green in CI). iOS, the `0.2.48` APK on the owner's phone, and the security headers IN PRODUCTION (tracked in `infra/nginx/`, but `GET /` still returns only `HTTP/1.1 200` — the VM config has not been applied). No deploy and no prod mutation this session.
- **The 429 fix proven three ways on the owner's live dev stack** — decisive one: ran the proof with **Docker not running at all** → 5/5 skipped, exit 0, `requireBackendUp` never reached, zero registrations. Then the shared run: **46 passed / 14 skipped, no 429**. Then the proof with its define: 4 of 5, the fifth failing LOCALLY ONLY on `DELPROOF-RECIPIENT-UNLINK` (id 977, 2026-09-14, `encryptedContent IS NULL`) — drill residue in a 9-day-old dev DB. CI starts empty and `E2E isolated probes` went green on `b2055eed`. Assertion NOT weakened, row NOT deleted.
- The ~890-line diff on `encryption_proof_test.dart` is `dart format` reindentation from the group wrap, not logic. `backend/package-lock.json` was dirty with the documented libc-only npm churn and was RESTORED, never committed.

## Notes for next session
- **The owner merged three of the four follow-ups THEMSELVES, mid-session** (`5f1b14a9` unreadable-reason, `4dac5585` the encryption proof, `f86a43de` the tracked prod vhost + document security headers, `1dfba82c` its CSP-hash fix). My rebase of `feat/unreadable-reason` produced an EMPTY diff — the branch had become master — so both branches and the `fireplace-e2eproof` worktree were swept like the rest (`ahead=0` confirmed first; SHAs in the findings file). `fireplace-csp` (detached `6ec287f4`) is the owner's live work, LEFT ALONE. Still open from that list: the `_reenrollAfterReset` latch, D10, and applying the vhost on the VM.
- **Then master went RED and I fixed it (`b2055eed`, CI 6/6).** The proof suite's 2 extra `/auth/register` calls tipped the shared `test_e2e` run over the 10-per-hour-per-IP bucket that `ci.yml:292-297` already recorded as spent to the edge, so `f86a43de` died on `ApiException(429)` in whichever suite ran last. Per that file's own rule ("never raise the production cap to fit a test") the proof moved to `e2e-isolated-probes` (2+2+2 = 6 of 10), gated on `--dart-define=ENCRYPTION_PROOF` with the lifecycle hooks INSIDE the group and the siblings' passing-test grep guard.
- Sealed sender is closed as a direction. If metadata work is wanted, it is RP1 (ephemeral-by-default) + RP2 (padding buckets) from the 2026-07-07 roadmap — and QW0/QW1/QW2 are already largely done (docker log caps `10m×3`, the L1 send log + L2 roster dump are gone, FCM `senderName` and the web-push topic are stripped; only `conversationId` remains).
- Owner-owed still open: signing-cert choice (PEPK vs sideload-forever) before the `0.2.48` APK is handed out; the `0.2.48` APK was never smoked (phone is on `0.2.47`); recovery-phrase replace A/B/C/D; iOS everywhere.
- Traps (appended to `docs/agents/traps.md`): the emoji-reaction decision blocking D10; a historical planning log's newest-first sections read as current state; `git worktree remove` fails on a backslash `gitdir` path; the shared `test_e2e` register budget is 10/hour and adding a suite to it is what turns an untouched file red; `addTearDown` inside `setUpAll` fires when `setUpAll` completes.
