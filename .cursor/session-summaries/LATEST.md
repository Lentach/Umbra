# Latest session summary

Newest first, **max 5 entries, each ≤ 900 chars** (`.githooks/pre-commit` enforces 5 and a hard 1000). Each entry links its dated file, which holds the full account. **Standing traps live in `docs/agents/traps.md`** — one line each, grouped by area; grep it for the area you are about to touch. Rotating an entry out of this file: move its `## Traps` lines to `traps.md` if not already there, then delete the entry. No banner, no history paragraphs here.

**DEPLOY STATE (2026-09-24 night: prod nginx gained `location /box/`, 32m scoped, backup `~/nginx-fireplace.bak.20260925T004016Z`; app tiers unchanged since 2026-09-23 evening):** **backend `0.2.51 / 14c1f22d`** (G4: migration `0022_box` applied, box OFF via `BOX_ENABLED: 'false'`; backup `chatdb-20260923T190351Z.dump.gpg` taken first; previous backend `9e621f63`, G2, 2026-09-21). Frontend unchanged at `0.2.50 / d37e2dcc`. Master now carries UNDEPLOYED frontend work (Phase 2a, storage-loss Part A, the PR0.2 client, the dark box client), so the next `deploy-web.ps1` needs its own gate. APK `0.2.51 / 20051` via `/apk/`; the owner's phone `f849cc68` runs it. Smoke 8/8. Rollback hazards + tags: `docs/agents/traps.md` § Deploy.

**Date:** 2026-09-24 — **The box media ladder has a 32 MiB rung, so a 20 MiB file that sends today keeps sending once PR3.1 moves media into the box (owner: raise now).** Backend `BOX_MEDIA_LADDER` + client `kBoxMediaLadder`; tracked nginx `location /box/` with `client_max_body_size 32m` scoped to that block and `proxy_request_buffering off`, then APPLIED on prod nginx on the owner's go-ahead (checkpoint `~/nginx-fireplace.bak.20260925T004016Z`): `/box/*` now reaches Nest (404, box still off), a 32 MiB POST passes, 33 MiB is 413, every other route unchanged, smoke 8/8. Proof: red 413 → int 24/24; CI 6/6 on `191fc86a`. Metadata-privacy decisions now live in ONE tracked log on the branch: read it before answering any such question. ➡ `2026-09-24-box-32mib-rung.md`.

**Date:** 2026-09-24 — **PR3.2 is on the branch: a device publishes a box request queue, and `searchUsers` hands a stranger each reachable device with a claimed bundle, its request queue and the signed list.** New `setRequestQueue`, migration `0023`, `claimBundle` as the one OTP door; search throttle now 100 / 15 min. Owner: peer `getDeviceList` stays until PR4.1; `authorization` in search; bundles with OTP. Proof: TDD, 7/7 + 4/4 SQL mutants, jest 1193, int 26, test_e2e 47/16, 1 reviewer APPROVE (2 findings fixed). Nothing on master/prod. Next: PR3.1. ➡ `2026-09-24-metadata-pr32-first-contact.md`.

**Date:** 2026-09-24 — **A2 done on the branch: the wire id is keyed by its authenticated SENDER, and an own row's wire id is the token this device minted.** `WireKey (senderId, wireId)`; `_wid`+`_wsid` stamped as one first-write-wins pair; decrypt stamps the session-authenticated sender, ack/lost-ack our own account with our minted token (review A: the server echo could pre-claim a sibling's message). Proof: TDD, 12/12 mutants, flutter 2371/14, ratchet 3163, CI 7/7 on `05aa4c69` + `a6de1f3a`, live Signal replay probe, and a two-browser release-web drive with the sealed records unsealed. Nothing on master/prod. Next: PR3.2. ➡ `2026-09-24-metadata-pr21-wire-sender.md`.

**Date:** 2026-09-24 — **PR2.1 is on the branch (draft PR #185, CI only): every message now carries a wire id, and two independent reviewers approved it.** It is `E2eEnvelope.msgId` (the sender's `sendToken`), `MessageModel.wireId` (never read from a server field, per review A1), a first-write-wins `_wid` stamp, and `wireIdIndex()` over the content store plus the legacy mobile store. Proof: TDD, 14 new tests, 13/13 mutants killed, flutter 2365/14, ratchet 3163, test_e2e 46/16, and a live two-account probe on the local stack. Open: A2 (the index carries no sender, so PR3.1 must dedup on (sender, wire id)). Nothing on master or prod. ➡ `2026-09-24-metadata-pr21-wire-id.md`.

**Date:** 2026-09-23 — **G4: the backend is live at `0.2.51 / 14c1f22d` with the box OFF, and the owner then ruled BRANCH UNTIL RELEASE N.** The review found the box has no global storage cap, so `BOX_ENABLED` is pinned `'false'` in prod (`27ec3421`). Proof: CI 6/6 on the tip, 0022 rehearsed on staging, prod `/box` refused, smoke 8/8. The 0.2.50 client was driven against the new backend: harness 44 passed (the 2 failures predate today); in the browser, messages, decrypt and ✓✓ work, and pushes behave as designed (visible → badge only, closed or background → push). Owned misses: no standalone statement at the gate, and a merge commit; branch sync is now rebase-only. Owed: a real prod push after a minute in the background, and a separate web-deploy gate. ➡ `2026-09-23-metadata-g4-gate.md`.

**Date:** 2026-09-23 — **A fresh session now picks up from a per-worktree, gitignored `NEXT.md` baton instead of a pasted handoff.** New skill `umbra-session-start` (drift check vs HEAD, LATEST fallback, report then wait, rename to `NEXT.consumed.md`) owns the baton format; `umbra-session-end` step 6 writes it after the push. OMP `/handoff` stays the in-conversation path. Pushed `23e86bfe` (docs only, rebased over 25 metadata commits). `fireplace-mp` got the skill as an UNTRACKED copy (delete it before mp merges master), proven discovered by a headless `omp` there. No round trip driven yet. ➡ `2026-09-23-session-start-skill.md`.


