# Latest session summary

Newest first, **max 5 entries, each ≤ 900 chars** (`.githooks/pre-commit` enforces 5 and a hard 1000). Each entry links its dated file, which holds the full account. **Standing traps live in `docs/agents/traps.md`** — one line each, grouped by area; grep it for the area you are about to touch. Rotating an entry out of this file: move its `## Traps` lines to `traps.md` if not already there, then delete the entry. No banner, no history paragraphs here.

**DEPLOY STATE (2026-09-24 night: prod nginx gained `location /box/`, 32m scoped, backup `~/nginx-fireplace.bak.20260925T004016Z`; app tiers unchanged since 2026-09-23 evening):** **backend `0.2.51 / 14c1f22d`** (G4: migration `0022_box` applied, box OFF via `BOX_ENABLED: 'false'`; backup `chatdb-20260923T190351Z.dump.gpg` taken first; previous backend `9e621f63`, G2, 2026-09-21). Frontend unchanged at `0.2.50 / d37e2dcc`. Master now carries UNDEPLOYED frontend work (Phase 2a, storage-loss Part A, the PR0.2 client, the dark box client), so the next `deploy-web.ps1` needs its own gate. APK `0.2.51 / 20051` via `/apk/`; the owner's phone `f849cc68` runs it. Smoke 8/8. Rollback hazards + tags: `docs/agents/traps.md` § Deploy.

**Date:** 2026-09-25 — **Sibling queues part A is on the branch (CI 7/7 on `dbe54d3a`), and every metadata-privacy decision now lives in ONE tracked log.** Part A (`a8a4e560`): own devices swap self-queue addresses through their request queues; a stranger's PreKey is refused before decrypt; a device on the link gate no longer publishes as the primary; proven live on a fresh link. `docs/plans/metadata-privacy-decisions.md` (1–33, OWNER vs ENGINEERING, batched per phase) + the PR3.1-remainder plan; owner answered 30–33 in one pass. Next: part B. ➡ `2026-09-25-metadata-sibling-queues-a.md`.

**Date:** 2026-09-24 — **The box media ladder has a 32 MiB rung, so a 20 MiB file that sends today keeps sending once PR3.1 moves media into the box (owner: raise now).** Backend `BOX_MEDIA_LADDER` + client `kBoxMediaLadder`; tracked nginx `location /box/` with `client_max_body_size 32m` scoped to that block and `proxy_request_buffering off`, then APPLIED on prod nginx on the owner's go-ahead (checkpoint `~/nginx-fireplace.bak.20260925T004016Z`): `/box/*` now reaches Nest (404, box still off), a 32 MiB POST passes, 33 MiB is 413, every other route unchanged, smoke 8/8. Proof: red 413 → int 24/24; CI 6/6 on `191fc86a`. Metadata-privacy decisions now live in ONE tracked log on the branch: read it before answering any such question. ➡ `2026-09-24-box-32mib-rung.md`.

**Date:** 2026-09-24 — **Decision (21) is built: a box send never looks up a device list; each connect verifies the covered peers' lists AND the own list once.** `BoxDeviceListRefresh`: a send awaits that connect's lookups, reads both lists from the cache, and fails the row ("Ponów") if a lookup is missing, failed or dropped; a failed lookup retries on its own backoff. A web↔AVD drive caught the first build fetching the OWN list at send time; it was fixed and re-driven: 0 lookups between typing and `BOX_SEND`, 0 server rows. 2509 tests pass; 25 mutants, the only survivor (a dead gate) deleted. Open: slice (d) must call `refreshBoxDeviceLists()` when it stores an address; next is the sibling-queue slice. ➡ `2026-09-24-metadata-pr31c-device-list-refresh.md`.

**Date:** 2026-09-24 — **PR3.1 slice (c) is on the branch: a text to a peer the box fully covers goes over the box and ONLY there — no server row.** Owner decisions 15–20: one path per message, all-or-nothing over both sides' devices, fail + retry under the same wire id, ✓ only when every frame was taken, composer limit 14 000 in every chat. Review/advisor/re-drive fixes: neither a down box nor an unverifiable peer list falls back to the old path; peer list re-verified on the first send per connect; in-flight retries; preview dropped, never text. Proof: 33 mutants (30 killed, 2 dead code deleted), flutter 2490/14, CI 7/7, web ↔ Pixel_7 drives on the final code, offline send → retry, 0 server rows. Next: sibling queues, then box push. ➡ `2026-09-24-metadata-pr31c-box-send.md`.

**Date:** 2026-09-24 — **PR3.1 slice (b) is on the branch: a box delivery is journaled, acked, decrypted under a LOCAL id, shown, and still there after a restart.** Owner decisions 12–14 (binary frame, sender clock clamped, local ids ≥ 2^48). `BoxInbox` never leaves a delivery unacked; `consumeBoxEntry` is the one dispatcher; server actions and reconciliation skip local ids (reconcile would have wiped every box message in 6 h). An independent review asked for 4 changes (raw-cache crowding, unretried acks, acks behind reads, re-key policy) — all fixed. Proof: 41/41 mutants, flutter 2459/14, CI 7/7, ratchet 3163, a browser drive and a Pixel_7 drive with a real peer (open chat, closed-chat badge, restart). Next: slice (c) send. ➡ `2026-09-24-metadata-pr31b-box-dispatcher.md`.


