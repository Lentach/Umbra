# Latest session summary

Newest first, **max 5 entries, each ≤ 900 chars** (`.githooks/pre-commit` enforces 5 and a hard 1000). Each entry links its dated file, which holds the full account. **Standing traps live in `docs/agents/traps.md`** — one line each, grouped by area; grep it for the area you are about to touch. Rotating an entry out of this file: move its `## Traps` lines to `traps.md` if not already there, then delete the entry. No banner, no history paragraphs here.

**DEPLOY STATE (OUR deploy, 2026-09-18 evening):** frontend serves `0.2.50 / d37e2dcc` (unchanged); **backend `0.2.51 / 353774b0`** — redeployed to ship the APK update channel, which `/version` now answers as `android {versionCode 20051, 0.2.51, https://fireplace.ignorelist.com/apk/umbra-0.2.51.apk}`. New nginx `location ^~ /apk/` → `~/fireplace/apk/`, a SIBLING of `frontend-build` because `deploy-web.ps1` `rm -rf`s that directory; mirrored into the tracked `infra/nginx/fireplace.conf` and diffed identical (199 lines both sides). `nginx -t` ok, reload clean, `/health` + `/` still 200. Backup `chatdb-20260918T191841Z.dump.gpg` predates it. **Owner's phone `f849cc68` is now `0.2.51 / 20051`**, updated in place from `20047` with `firstInstallTime` 2026-09-13 unchanged. Earlier the same day: reactions released as `0.2.49 / ac2fd98a` (migration `0018`, backup `chatdb-20260918T024051Z.dump.gpg`) and the PWA boot loader as `0.2.50 / d37e2dcc`, smoke 7/7. Rollback hazards + tags: `docs/agents/traps.md` § Deploy.

**Date:** 2026-09-23 — **Owner dropped storage-loss Parts B and C (never committed); Part A's post-T hide is reverted so a dead-session message shows "Nie można odczytać" instead of vanishing, and the cold-start own-key self-alarm is fixed ((lxxxix)).** `5e931ad0`, `ca018545` (list pin), `917845eb`; red-first + mutants each; flutter 2318/14, analyze 3163, harness 46/14. CI 7/7 on `ca018545`; `917845eb` Backend RED on a real `recoverPassword` timer bug (fix first, unasked commit). PR1.1 not started. NOT deployed, NOT on master. ➡ `2026-09-23-storage-loss-drop-bc.md`.

**Date:** 2026-09-23 — **A wiped PWA no longer shows "Nie można odczytać" or "Wiadomość zaszyfrowana" (Part A `f8a81f23`); the keys-come-back backup (lever d) and re-delivery (Parts B/C) were built, then DROPPED by the owner uncommitted.** PR1.0 `efba8a72` (+ unasked throttler 6.7.0 `77c550c9`, blank X-Real-IP `1cc12aa7`), gaps 1-3, Gap 5 drive. Live cache-clear repro: contacts were BLOCKED from sending until fingerprints matched — still true. Part A on a clean tree: flutter 2318/14, analyze floor 3163. B/C snapshot: `.planning/metadata-privacy/wip-partB-partC-2026-09-23.zip`. NOT deployed, NOT on master. ➡ `2026-09-23-storage-loss-part-a.md`.

**Date:** 2026-09-22 — **Lever (b) + PR0.2: a re-minted linking-OFF install now shows ONE "Historia sprzed połączenia" divider instead of a wall of "Nie można odczytać", and the server no longer learns which chat is open.** (lxxxvi): boundary = the server audit instant that ended at our key, persisted, re-asked after the upload; web drive: 13 pre-loss rows → one divider, survives reload. (lxxxvii) closed a takeover-alarm hole (an unspent one-shot ate the next foreign replacement). PR0.2 `482563f5`: `pushClientState {clientVisible}` only, push skipped while visible on any chat. jest 1155, flutter 2309, harness 46/14, CI 7/7 on `482563f5`. Keys still re-mint on linking OFF — owner-owed. NOT deployed, NOT on master. ➡ `2026-09-22-metadata-lever-b-pr02.md`.

**Date:** 2026-09-22 — **Run C driven on device + web: a phrase restore keeps the keys, but every LIVE peer was wedged until it reloaded — fixed client-side; the loss notice no longer covers the phrase door, and the door now sits above the reset.** Four restores (device 1→3→5→7→9), identity byte-identical, backup rev 19 untouched, no pill, no placeholders. The peer held the old device list for >5 min (not "~90 s"): sends failed `no key bundle`, inbound withheld, a reload dropped the unsent row. F1/F2/F3 = one rate-limited verified refetch on a dead-address send, on an unknown origin device, on `sessionRebuildNeeded`; re-driven live (retry in 4 s, inbound with no reload). Owner: S2 residual accepted, S1 deferred to PR4.2. flutter 2304/14, ratchet 3165, **CI 7/7 on `35f5414f`** (PR #184). NOT deployed; master `2ab9265c`. ➡ `2026-09-22-metadata-run-c-close-2a.md`.

**Date:** 2026-09-22 — **A logout then login FROZE the contact backup until app restart — shipped at G2, caught on the owner's iPhone, fixed in one line.** `AuthProvider.logout` calls `ContactBackupService.clear`, which nulls `_store` + `store.onChanged`; re-login only calls `onSession`, and `attach` was reachable only from `setProviders` at widget build — so `uploadNow` bailed on `store == null` and every mutation notified nobody, silently. `connection_provider.dart:428` re-arms it per session, OUTSIDE the `isOpen` gate (gating re-freezes a slow open). Red-first + mutant; the first red was wrong (cooldown swallowed connect). flutter 2297/14; live: logout→login→mute moved `rev` 16→18. `persist()` IS granted on iOS PWA. **NOT deployed, NOT on master (`2ab9265c`), 0 check-runs on `12d9ec3d`.** ➡ `2026-09-22-metadata-logout-relogin-backup-freeze.md`.

