# Latest session summary

Newest first, **max 5 entries, each ≤ 900 chars** (`.githooks/pre-commit` enforces 5 and a hard 1000). Each entry links its dated file, which holds the full account. **Standing traps live in `docs/agents/traps.md`** — one line each, grouped by area; grep it for the area you are about to touch. Rotating an entry out of this file: move its `## Traps` lines to `traps.md` if not already there, then delete the entry. No banner, no history paragraphs here.

**DEPLOY STATE (OUR deploy, 2026-09-18 evening):** frontend serves `0.2.50 / d37e2dcc` (unchanged); **backend `0.2.51 / 353774b0`** — redeployed to ship the APK update channel, which `/version` now answers as `android {versionCode 20051, 0.2.51, https://fireplace.ignorelist.com/apk/umbra-0.2.51.apk}`. New nginx `location ^~ /apk/` → `~/fireplace/apk/`, a SIBLING of `frontend-build` because `deploy-web.ps1` `rm -rf`s that directory; mirrored into the tracked `infra/nginx/fireplace.conf` and diffed identical (199 lines both sides). `nginx -t` ok, reload clean, `/health` + `/` still 200. Backup `chatdb-20260918T191841Z.dump.gpg` predates it. **Owner's phone `f849cc68` is now `0.2.51 / 20051`**, updated in place from `20047` with `firstInstallTime` 2026-09-13 unchanged. Earlier the same day: reactions released as `0.2.49 / ac2fd98a` (migration `0018`, backup `chatdb-20260918T024051Z.dump.gpg`) and the PWA boot loader as `0.2.50 / d37e2dcc`, smoke 7/7. Rollback hazards + tags: `docs/agents/traps.md` § Deploy.

**Date:** 2026-09-23 — **A fresh session now picks up from a per-worktree, gitignored `NEXT.md` baton instead of a pasted handoff.** New skill `umbra-session-start` (drift check vs HEAD, LATEST fallback, report then wait, rename to `NEXT.consumed.md`) owns the baton format; `umbra-session-end` step 6 writes it after the push. OMP `/handoff` stays the in-conversation path. Pushed `23e86bfe` (docs only, rebased over 25 metadata commits). `fireplace-mp` got the skill as an UNTRACKED copy (delete it before mp merges master), proven discovered by a headless `omp` there. No round trip driven yet. ➡ `2026-09-23-session-start-skill.md`.

**Date:** 2026-09-21 — **The owed G2 drive passed on `27edf4f1` (device + web + import half + passcode re-lock), and it caught two leaks both reviews missed — now FIXED.** (1) A pure restore republished the blob it had just downloaded, moving `rev`/`updatedAt`: a "this account lost its storage at T" clock. `_adopt` now seeds the no-op fingerprint from the opened payload and `toJson` sorts contacts, so a wiped device re-drive left the row at rev 3. Regression red-before-green. (2) The export filename was `umbra-<userId>-<date>.umbrabak` — an account id into Drive/Gmail in cleartext; now `umbra-backup-<date>`. Drive proof (BOTH surfaces re-driven post-fix; web control = a week-long mute that survived): restore before `IDENTITY_MINTED`, offline hydrate, export→wipe→import `{records: 2}`, locked vault touches no row. flutter 2288/14, ratchet 3166. **G2 still owner-owed.** ➡ `2026-09-21-metadata-phase2a-device-web-drive.md`.

**Date:** 2026-09-20 — **PR2.4/PR2.3 were reviewed by two independent agents, both REQUEST-CHANGES, and every finding was real — folded in `1cfdc7f2`; CI 7/7 green on `3a6f25ad`.** Eight never-clobber holes (a 409 retry discarded the wrap being added AND republished a stale blob; `applyRestore` ignored a refused write; the backend rev guard was check-then-save; `resetPassword` ignored its re-wrap refusal) and four privacy leaks (blob length was a contact counter — now 4 KiB padded; the server's no-op guard could never fire under a fresh IV, so `updatedAt` moved on every upload; the 4 mb body limit was global; history import accepted the whole `e2e_<uid>_` namespace). jest 1156/66, flutter 2287/14, harness 46/14, ratchet 3166; live wire: two concurrent PUTs now answer 200/409. **Owed before G2: re-run the Pixel_7 `pm clear` drive** — the restore path changed. ➡ `2026-09-20-metadata-pr24-pr23-review-fixes.md`.

**Date:** 2026-09-20 — **Metadata privacy Phase 2a is COMPLETE on `feat/metadata-privacy` (`3915f4f8`, draft PR #182), NOT merged — G2 is owner-owed.** PR2.4 server-held contact backup (opaque row, CK wrapped under PBKDF2-600k of the PASSWORD, `rev` concurrency over the whole triple, no-op PUT moves no clock) + PR2.3 user-held history file (`share_plus` export, native import, storage-loss screen). Proof: jest 1151/66, flutter 2281/14, harness 46/14, ratchet held 3166; live wire drove 404→create→no-op→409 stale→409 salt→1.5 MB→cascade; **Pixel_7 `pm clear` → login → `CONTACT_BACKUP_RESTORED {count: 2}` before `IDENTITY_MINTED`, both chats back**; export sheet produced a real sealed `.umbrabak`. Two self-found bugs fixed (unhandled async KDF error; a latch no widget could observe). Next: PR0.2, then PR1.0. ➡ `2026-09-20-metadata-pr24-pr23-backups.md`.

**Date:** 2026-09-20 — **G0 is LIVE (master `f27c5712` = prod backend, 0019/0020 applied, 115 accounts) and PR2.2 `ContactStore` is committed on `feat/metadata-privacy` (`9c69c34b`, draft PR #182 for CI), NOT merged.** Client-owned contact list in the message store, hydrated before the socket, written through by every list handler; two independent reviews folded (prune-vs-upsert race, skeleton gate, blocked ghost, open budget, foreign-version overwrite, lock-restore symmetry). Proof: analyze 0, ratchet held, flutter 2243/14, harness 46/14, Pixel_7 cold start offline listed 2 chats + 2 contacts from SQLCipher. Owner deleted every line budget from the plan. Next: PR2.4 backup (cache CK, not the derived key). ➡ `2026-09-20-metadata-pr22-contact-store.md`.


