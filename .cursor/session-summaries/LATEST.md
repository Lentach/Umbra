# Latest session summary

Newest first, **max 5 entries, each ≤ 900 chars** (`.githooks/pre-commit` enforces 5 and a hard 1000). Each entry links its dated file, which holds the full account. **Standing traps live in `docs/agents/traps.md`** — one line each, grouped by area; grep it for the area you are about to touch. Rotating an entry out of this file: move its `## Traps` lines to `traps.md` if not already there, then delete the entry. No banner, no history paragraphs here.

**DEPLOY STATE (OUR deploy, 2026-09-14):** both tiers serve `0.2.48 / eefc44b0` — CI **6/6 on the code-bearing commit `1f05e3e6`** (the tip is docs-only, so `paths-ignore` leaves it 1 row — check the code commit, never the tip). Backend health `starting → healthy` in 10 s, `fireplace-db-1` NOT recreated; web smoke **5/5** incl. the `main.dart.js` contains-`eefc44b0` stale-build gate; `/version` + `/version.json` both `0.2.48 / eefc44b0`, `/health` `{"status":"ok","db":"ok"}`. The guard string `Only the sender can delete for everyone` is present **2×** in the RUNNING container's `dist/`. The owner's phone APK stays **0.2.47 / 20047** (a web deploy does not touch it). Rollback hazards + tags: `docs/agents/traps.md` § Deploy.

**Date:** 2026-09-18 — **Drove `feat/reaction-tokens` for the first time and it was broken exactly where falsification R7 said: after a relaunch every chip rendered the unreadable placeholder on a device holding the key.** Two post-install clobbers fixed: a cold history entry awaits plaintext hydration, so the LOCAL key lands before the rows reach `_messages` and the re-render spent its latched attempt on nothing; then the decrypt pass merged the RAM decryption cache, whose `server.copyWith(...)` reactions won (why INBOUND rows stayed placeholders while own rows resolved). Proven on Chrome AND an Android emulator against staging with `0018`: only 22-char tokens at rest, peer render and relaunch correct. Merged master, resolved the design-doc conflict, **CI 7/7 on `f1311a38`**, PR #179 MERGEABLE. **Not merged to master — owner's call.** ➡ `2026-09-18-reaction-tokens-driven.md`.

**Date:** 2026-09-18 — **PWA cold-start is a full multi-MB shell re-download behind a blank screen; a boot loader now makes the wait legible (PR-only, NOT deployed).** No offline cache (Flutter 3.44 SW self-unregisters on `activate`) + nginx `no-cache` on `/` → any idle-return/post-deploy launch re-fetches `main.dart.js` 7.6 MB + local `canvaskit.wasm` 6.9 MB (~15 s on mobile); daily users stay warm in the HTTP cache. `web/index.html` `#fp-boot` + external `web/fp_boot.js` (CSP-clean via `'self'`, no third hash): shield + determinate fake-progress bar to 99% + Polish-default label, removed on `flutter-first-frame`. Proven in headless Chromium — script runs and self-removes, no brick. Bigger owner-owed fix: gzip/brotli + immutable `/canvaskit/`. ➡ `2026-09-18-session-pwa-boot-loader.md`.

**Date:** 2026-09-17 — **Reactions are blinded end to end on `feat/reaction-tokens`, and two independent reviews turned up seven ways the wiring DESTROYED key material rather than seven ways the feature misbehaved.** A tap now sends a 22-char token and never degrades to plaintext. Fixed: publishing `K_react` before persisting it; deciding "absent" from the per-engine cache (the 2026-07-29 mechanism, and absence is what spends the one-shot mailbox row); guessing the fan-out; an uncorrelated upload ack; a timeout-less shared fetch; a double tap double-decrypting one row; an unremovable legacy plaintext reaction. Silence from an upload is now reconciled, not rolled back. 2192 flutter / 1159 jest, two mutants killed. **NOTHING was run: no browser, no emulator, no stack.** ➡ `2026-09-17-reaction-tokens-end-to-end.md`.

**Date:** 2026-09-17 — **The app document has security headers at last, the prod vhost is TRACKED (which is why it went months without them), and the CSP survived a logged-in drive.** `frontend/nginx.conf` was never what users load. `infra/nginx/` is the source of truth now: HSTS/nosniff/DENY/Referrer-Policy/COOP/Permissions-Policy + REPORT-ONLY CSP, on the two document blocks ONLY (every proxied path already carries helmet's, measured). Three real defects came out of the report stream, two of them only findable logged in: `base-uri 'none'` rejects Flutter's own `<base>`; the script hashes were CRLF digests (parsers normalize to LF); and **under CanvasKit `connect-src`, not `img-src`, governs remote images** — an `api.giphy.com`-only policy meant an EMPTY GIF picker. Zero violations after. ➡ `2026-09-17-session-security-headers.md`.

**Date:** 2026-09-17 — **The codelock + multi-device program was already FINISHED; what was left was litter and two stale tracking docs.** `feat/passcode-lock` is byte-identical to `origin/master`; tree green with ZERO drift (backend 1127, flutter 2144/14, ratchet 898, `tsc` 0); CI 6/6 on `1f05e3e6`; prod re-measured today `0.2.48 / eefc44b0`, `/health` ok. A fresh audit reported D1 (P0) + D2 (P1) as open — both are FIXED under (xlvii); the newest-first planning log misled it and now carries a `⛔ HISTORICAL LOG` banner. Swept **25→5 local / 21→6 remote branches, 7→3 worktrees**, every deletion content-verified against master; deleted the 2-month-stale `docs/ISSUE-BOARD.md` (its live item moved to traps). **Sealed sender DECLINED** on the repo's own two audits; owner takes headers / D10 / the reenroll latch / two branch merges next. ➡ `2026-09-17-session-state-audit-cleanup.md`.
