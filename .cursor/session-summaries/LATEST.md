# Latest session summary

Newest first, **max 5 entries, each ≤ 900 chars** (`.githooks/pre-commit` enforces 5 and a hard 1000). Each entry links its dated file, which holds the full account. **Standing traps live in `docs/agents/traps.md`** — one line each, grouped by area; grep it for the area you are about to touch. Rotating an entry out of this file: move its `## Traps` lines to `traps.md` if not already there, then delete the entry. No banner, no history paragraphs here.

**DEPLOY STATE (OUR deploy, 2026-09-14):** both tiers serve `0.2.48 / eefc44b0` — CI **6/6 on the code-bearing commit `1f05e3e6`** (the tip is docs-only, so `paths-ignore` leaves it 1 row — check the code commit, never the tip). Backend health `starting → healthy` in 10 s, `fireplace-db-1` NOT recreated; web smoke **5/5** incl. the `main.dart.js` contains-`eefc44b0` stale-build gate; `/version` + `/version.json` both `0.2.48 / eefc44b0`, `/health` `{"status":"ok","db":"ok"}`. The guard string `Only the sender can delete for everyone` is present **2×** in the RUNNING container's `dist/`. The owner's phone APK stays **0.2.47 / 20047** (a web deploy does not touch it). Rollback hazards + tags: `docs/agents/traps.md` § Deploy.

**Date:** 2026-09-17 — **The codelock + multi-device program was already FINISHED; what was left was litter and two stale tracking docs.** `feat/passcode-lock` is byte-identical to `origin/master`; tree green with ZERO drift (backend 1127, flutter 2144/14, ratchet 898, `tsc` 0); CI 6/6 on `1f05e3e6`; prod re-measured today `0.2.48 / eefc44b0`, `/health` ok. A fresh audit reported D1 (P0) + D2 (P1) as open — both are FIXED under (xlvii); the newest-first planning log misled it and now carries a `⛔ HISTORICAL LOG` banner. Swept **25→5 local / 21→6 remote branches, 7→3 worktrees**, every deletion content-verified against master; deleted the 2-month-stale `docs/ISSUE-BOARD.md` (its live item moved to traps). **Sealed sender DECLINED** on the repo's own two audits; owner takes headers / D10 / the reenroll latch / two branch merges next. ➡ `2026-09-17-session-state-audit-cleanup.md`.

**Date:** 2026-09-14 — **Both halves of the media-authorization + identity-banner fix are LIVE, and the E2E claim now has a measured gap list.** Deployed `1f05e3e6` to both tiers; proved the fix EXECUTES (guard string 2× in the running container's `dist/`), not merely ships. Proved attachments are encrypted client-side three ways: 24 blobs all predating the session, `createCipheriv` absent from all `backend/src`, entropy 7.999 b/B with no container magic. Corrected two over-claims: a peer key change BLOCKS sends, and the DAK-signed device list closes ghost devices. Gap list filed at `docs/audit/2026-09-14-e2e-gap-audit.md` — **local, untracked**, never copy its specifics into a tracked file. ➡ `2026-09-14-e2e-gap-audit-and-deploy.md`.

**Date:** 2026-09-14 — **Delete-for-everyone is irreversible on devices — and a RECIPIENT could destroy the sender's media file.** Drove it on an emulator + a live peer: row and envelopes gone (FK CASCADE), the peer's OPEN chat dropped it live, the sender's on-device `stored` set went `[974]`→`[]`, and a wiped-storage fresh login gets nothing back. The media blob is unlinked from `/app/media/msgs`. The bug: `handleDeleteMessage` unlinked BEFORE the sender check (membership was the only gate), so a recipient destroyed the file and left a row with a dangling `mediaUrl` — fixed, `wire.md` updated, Jest + widget regressions proven red-first. Also fixed the owner-reported identity banner painting UNDER the glass top bar in an EMPTY chat. Suites 1127 / 2144. Re-ru…

**Date:** 2026-09-14 — **Deleting a message now reaches every device it must, "clear chat" admits it wipes both sides, and 0.2.47 is live on both tiers.** `messageDeleted`/`chatHistoryCleared` had emitted to the acting socket + the PEER's room, so the deleter's own devices never heard — and the history merge never prunes, so the plaintext stayed on screen. Now both rooms (`for_me` → the hider's room only). The `delete_forever` hold is a real abortable guard but disclosed nothing, so it gained a tooltip, a scope-stating caption and `clear_chat_dialog.dart`. I proposed server-side tombstones for offline convergence, built them, then **reverted all of 0018**: `getServedMessageIds`/`reconcileStoredPlaintext` already does it, and a ledger of deletions is a privacy liability; the real remainder was the reconcile leaving the ROW behind. 1126/2143 green, floor 3168, smoke 5/5. ➡ `2026-09-14-session-delete-convergence.md`.

**Date:** 2026-09-14 — **The shared APK halves to 48.9 MiB with nothing removed; the repo is now `Lentach/Umbra` and friends get a hand-delivered file, not a GitHub Release.** `useLegacyPackaging` compresses the 24 native libs (100.29 → 44.19 MiB); every ABI still ships. 0.2.47 `320c11dd` PROVED it on the owner's phone: cert digest = record-of-truth, 16KB **16/16 on the packed APK**, `firstInstallTime` survived a FIFTH in-place upgrade, clean arm64 cold start, device footprint FELL 105.3 → 82.9 MiB (only one ABI extracts). 0.2.48 `75601dc4` is the friends file (`976106d7…`) but is **NOT smoked** — both devices unplugged mid-build. Declined: ABI narrowing and public releases, so there is NO auto-update. Owed before sending: the PEPK-vs-sideload-forever cert choice. ➡ `2026-09-14-session-apk-weight.md`.
