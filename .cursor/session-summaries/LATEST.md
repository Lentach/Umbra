# Latest session summary

Newest first, **max 5 entries, each ≤ 900 chars** (`.githooks/pre-commit` enforces 5 and a hard 1000). Each entry links its dated file, which holds the full account. **Standing traps live in `docs/agents/traps.md`** — one line each, grouped by area; grep it for the area you are about to touch. Rotating an entry out of this file: move its `## Traps` lines to `traps.md` if not already there, then delete the entry. No banner, no history paragraphs here.

**DEPLOY STATE (OUR deploy, 2026-09-18 evening):** frontend serves `0.2.50 / d37e2dcc` (unchanged); **backend `0.2.51 / 353774b0`** — redeployed to ship the APK update channel, which `/version` now answers as `android {versionCode 20051, 0.2.51, https://fireplace.ignorelist.com/apk/umbra-0.2.51.apk}`. New nginx `location ^~ /apk/` → `~/fireplace/apk/`, a SIBLING of `frontend-build` because `deploy-web.ps1` `rm -rf`s that directory; mirrored into the tracked `infra/nginx/fireplace.conf` and diffed identical (199 lines both sides). `nginx -t` ok, reload clean, `/health` + `/` still 200. Backup `chatdb-20260918T191841Z.dump.gpg` predates it. **Owner's phone `f849cc68` is now `0.2.51 / 20051`**, updated in place from `20047` with `firstInstallTime` 2026-09-13 unchanged. Earlier the same day: reactions released as `0.2.49 / ac2fd98a` (migration `0018`, backup `chatdb-20260918T024051Z.dump.gpg`) and the PWA boot loader as `0.2.50 / d37e2dcc`, smoke 7/7. Rollback hazards + tags: `docs/agents/traps.md` § Deploy.

**Date:** 2026-09-19 — **The god-class refactor was re-verified and then DROPPED by the owner; D10 is closed; knip now sees unused exports.** Five scouts checked plan v2 against live code: the socket-registry PR is net 20–55 lines, knip defaults flag 24 transitive `socket.io` imports, the PR1 seam is just `ContentKv`. v3 was written, then judged pure motion — `task_plan.md` opens with a DROPPED banner and stays as a verified cluster map. Shipped in `84d9608d` (backend, NOT deployed): reactions accept the 22-char token only (`chat.dto.ts`, jest 1164→1140), `knip` with defaults + `socket.io` listed + dead `socketsForUser` gone, ratchet 897. A hard min-version gate was built, rendered, and reverted on the owner's word (banner is enough while testing; Play tracks auto-update). Owed: `deploy-backend.sh`. ➡ `2026-09-19-refactor-dropped-d10-closed.md`.

**Date:** 2026-09-19 — **Metadata privacy researched and PARKED; a god-class refactor was planned, independently reviewed, and handed to a FRESH agent — start at `.planning/refactor-godclasses/task_plan.md` §Handoff.** Sealed sender alone is theater on 1:1 + a server-side `conversations` table; the foundation is server→transient-mailbox, skeleton in `.planning/metadata-privacy/findings.md` (untracked). Refactor v2: PR0a knip → PR0 dead stub + phone probe → PR0-web → PR1 tenants → PR1.5 socket registry → PR2 PlaintextRecordStore → PR3 → min-version gate → PR-D10. Reviewer killed half of v1's delete list (live callers, live Android path). Baseline GREEN this session: flutter 2215/14 skipped, jest 1164. Owner: composer freeze lifted, PWA state disposable, Android first, phone usable read-only. No code changed, nothing deployed. ➡ `2026-09-19-metadata-research-and-refactor-plan.md`.

**Date:** 2026-09-18 — **Sideloaded Android users can now be TOLD a newer APK exists, and the install→update path was finally driven on a device instead of described.** `GET /version` gained a separate `android` block (env-fed, `null` until published — deliberately NOT the web semver, which deploys alone); Settings offers it with a per-build Later. Emulator, release-signed, cert = the runbook record-of-truth: saw the offer on `20049`, `adb install -r` → `20050` with `firstInstallTime` UNCHANGED, relaunch still LOGGED IN. The first install refused with `INSTALL_FAILED_UPDATE_INCOMPATIBLE` over a debug-signed build — the PEPK trap, reproduced. A later run proved the rest on-device: a pre-update message still decrypted after the update, and one sent FROM the updated build decrypted on the peer — history, SQLCipher key and ratchet all survive. The offer is now a banner in `main_shell`; the Settings card went as a duplicate. CI 6/6 on `6a9475ab`. ➡ `2026-09-18-apk-update-check.md`.

**Date:** 2026-09-18 — **Drove `feat/reaction-tokens` for the first time and it was broken exactly where falsification R7 said: after a relaunch every chip rendered the unreadable placeholder on a device holding the key.** Two post-install clobbers fixed: a cold history entry awaits plaintext hydration, so the LOCAL key lands before the rows reach `_messages` and the re-render spent its latched attempt on nothing; then the decrypt pass merged the RAM decryption cache, whose `server.copyWith(...)` reactions won (why INBOUND rows stayed placeholders while own rows resolved). Proven on Chrome AND an Android emulator against staging with `0018`: only 22-char tokens at rest, peer render and relaunch correct. Merged master, resolved the design-doc conflict, **CI 7/7 on `f1311a38`**, PR #179 MERGEABLE. **Not merged to master — owner's call.** ➡ `2026-09-18-reaction-tokens-driven.md`.

**Date:** 2026-09-18 — **PWA cold-start is a full multi-MB shell re-download behind a blank screen; a boot loader now makes the wait legible — LIVE as `0.2.50 / d37e2dcc`.** No offline cache (Flutter 3.44 SW self-unregisters on `activate`) + nginx `no-cache` on `/` → any idle-return/post-deploy launch re-fetches `main.dart.js` 7.6 MB + local `canvaskit.wasm` 6.9 MB (~15 s on mobile); daily users stay warm in the HTTP cache. `web/index.html` `#fp-boot` + external `web/fp_boot.js` (CSP-clean via `'self'`, no third hash): shield + determinate fake-progress bar to 99% + Polish-default label, removed on `flutter-first-frame`. CI 5/5, smoke 7/7. RISK: if `fp_boot.js` fails to load the opaque overlay never lifts — add a Dart-side removal net. Bigger fix: gzip/brotli + immutable `/canvaskit/`. ➡ `2026-09-18-session-pwa-boot-loader.md`.

